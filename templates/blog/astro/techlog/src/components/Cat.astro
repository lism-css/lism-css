---
import { Inline, Link } from 'lism-css/astro';

interface Props {
  href?: string;
  class?: string;
}

const { href, class: className, ...rest } = Astro.props;

const Tag = href ? Link : Inline;
const linkProps = href ? { href, set: 'plain', hov: '-bgc' } : {};
---

<Tag class:list={['c--cat', className]} set="var:s" bgc="base-2" fz="2xs" lh="xs" py="5" px="10" bdrs="10" {...linkProps} {...rest}>
  <slot />
</Tag>
