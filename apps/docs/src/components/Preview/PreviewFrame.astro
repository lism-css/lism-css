---
import { Stack, Frame, Flex, Lism, Icon } from 'lism-css/astro';
import { Button } from '@lism-css/ui/astro/Button';
import { DesktopIcon, DeviceMobileCameraIcon, DeviceTabletCameraIcon } from '@phosphor-icons/react';
import { getLangFromUrl, t } from '@/lib/i18n';

/**
 * iframe プレビュー。
 * 高さは iframe 内の PreviewSizeReporter から通知される中身の高さにフィットさせ、
 * 上限は --ar（アスペクト比）から求めた高さ。上限を超える分は iframe 内でスクロールする。
 */
interface Props {
  [key: string]: any;
}
const { hasSizeSwitch = false, hasExternalLink = true, externalLinkLabel, url, ...props } = Astro.props;

const lang = getLangFromUrl(Astro.url);
const externalLabel = externalLinkLabel ?? t(lang, 'demo').openNewTab;
const previewText = t(lang, 'preview');

// wPct: iframe を表示幅より広いビューポートで描画して zoom で縮める割合（未指定なら _preview.scss の既定値）
const sizes = [
  { id: 'mobile', label: previewText.mobile, icon: DeviceMobileCameraIcon, size: '360px', ar: '2/3', wPct: '120' },
  { id: 'tablet', label: previewText.tablet, icon: DeviceTabletCameraIcon, size: '600px', ar: '3/4' },
  { id: 'desktop', label: previewText.desktop, icon: DesktopIcon, size: '100%', ar: '3/2' },
];
const defaultSize = 'desktop';
---

<Stack g="20">
  {
    hasSizeSwitch && (
      <Flex
        jc="center"
        ai="center"
        p="5"
        g="5"
        bgc="base-2"
        bdrs="20"
        set="bdrsInner"
        w="fit-content"
        mx="auto"
        class="c--previewSizeSwitcher"
        role="group"
        aria-label={previewText.sizeAriaLabel}
        data-preview-url={url}
      >
        {sizes.map((size) => (
          <Lism
            as="button"
            type="button"
            aria-pressed={size.id === defaultSize ? 'true' : 'false'}
            aria-label={size.label}
            title={size.label}
            fz="l"
            p="0.375rem"
            bd="none"
            bdrs="inner"
            class="c--previewSizeSwitcher_btn"
            data-size={size.size}
            data-ar={size.ar}
            data-w-pct={size.wPct}
          >
            <Icon as={size.icon} fz="l" d="block" />
          </Lism>
        ))}
      </Flex>
    )
  }

  {/* 角丸・枠線・影は zoom のかかる内側ではなく、外側でまとめて指定する（影の角が角丸からはみ出さないように） */}
  <Frame
    id="preview-iframe"
    class="c--previewFrame"
    data-preview-url={url}
    bxsh="10"
    bdrs="20"
    bd
    bdw="2px"
    mx="auto"
    w="100%"
    max-w="100%"
    style={{ '--ar': '3/2' }}
  >
    {/* ar は高さ通知が届くまで（または JS 無効時）のフォールバック。通知後は iframe に height を直接指定する */}
    <Frame className="u--scalePreview" ar {...props}>
      <iframe src={url} width="800" height="600" style="border:0"></iframe>
    </Frame>
  </Frame>
  {
    hasExternalLink && (
      <Button href={url} variant="fill" target="_blank" rel="noopener noreferrer" bdrs="10" w="fit" mx="auto">
        {externalLabel}
      </Button>
    )
  }
</Stack>

<style>
  @layer lism-custom {
    /* Chrome で iframe 内の backdrop-filter 層が overflow:hidden の角丸クリップを抜けるため、filter で合成グループ化して丸ごと切る（見た目への影響なし） */
    .c--previewFrame {
      filter: opacity(1);
    }

    .c--previewSizeSwitcher_btn {
      cursor: pointer;
      color: var(--text-2);
      background-color: transparent;
    }
    .c--previewSizeSwitcher_btn[aria-pressed='true'] {
      background-color: var(--base);
      color: var(--text);
      box-shadow: var(--bxsh--10);
    }
  }
</style>

<script>
  const SIZE_TYPE = 'lism-preview-size';
  const REQUEST_TYPE = 'lism-preview-size-request';

  // '3/2' → 1.5
  const parseRatio = (value: string): number => {
    const [w, h] = value.split('/').map((n) => parseFloat(n));
    return w > 0 && h > 0 ? w / h : 3 / 2;
  };

  document.querySelectorAll<HTMLElement>('.c--previewFrame').forEach((frame) => {
    const scaled = frame.querySelector<HTMLElement>('.u--scalePreview');
    const iframe = frame.querySelector('iframe');
    if (!scaled || !iframe) return;

    // iframe 内の CSS px で受け取る（width は iframe 内の innerWidth）
    let contentSize: { width: number; height: number } | null = null;

    const applyHeight = () => {
      if (!contentSize?.width) return;
      const ratio = parseRatio(getComputedStyle(frame).getPropertyValue('--ar'));
      // zoom を iframe 内へ伝えない Safari 26.3 以前では、iframe 内の innerWidth（表示幅）が
      // 親の zoom 空間でのレイアウト幅（offsetWidth）より小さくなるので、zoom の逆数（--w-pct / 100）で補正する。
      // 比を実測値から出すと offsetWidth の整数丸めで 1px 未満のはみ出しが起きるため、既知の倍率を使う。
      const zoomInherited = Math.abs(iframe.offsetWidth - contentSize.width) <= 1;
      const wPct = parseFloat(getComputedStyle(scaled).getPropertyValue('--w-pct')) || 100;
      const scale = zoomInherited ? 1 : wPct / 100;
      const layoutWidth = contentSize.width * scale;
      const maxHeight = layoutWidth / ratio;
      // 高さは iframe 自体に指定し、Frame 側は比率を解除して中身に合わせる
      scaled.style.aspectRatio = 'auto';
      iframe.style.height = `${Math.min(contentSize.height * scale, maxHeight)}px`;
    };

    window.addEventListener('message', (event) => {
      if (event.origin !== window.location.origin) return;
      if (event.source !== iframe.contentWindow || event.data?.type !== SIZE_TYPE) return;
      contentSize = { width: event.data.width, height: event.data.height };
      applyHeight();
    });

    // サイズ切替
    const url = frame.dataset.previewUrl ?? '';
    const switcher = document.querySelector(`.c--previewSizeSwitcher[data-preview-url="${url}"]`);
    if (!switcher) return;

    const buttons = switcher.querySelectorAll<HTMLElement>('.c--previewSizeSwitcher_btn');
    buttons.forEach((button) => {
      button.addEventListener('click', () => {
        frame.style.width = button.dataset.size ?? '';
        frame.style.setProperty('--ar', button.dataset.ar ?? '');
        if (button.dataset.wPct) {
          scaled.style.setProperty('--w-pct', button.dataset.wPct);
        } else {
          scaled.style.removeProperty('--w-pct');
        }
        buttons.forEach((b) => b.setAttribute('aria-pressed', b === button ? 'true' : 'false'));

        // 幅・比率が変わったので、レイアウト後の値を iframe 側に問い合わせて高さを更新する
        iframe.contentWindow?.postMessage({ type: REQUEST_TYPE }, window.location.origin);
      });
    });
  });
</script>
