---
title: 'Getting Started'
description: '...'
---
import { Preview, PreviewArea, PreviewTitle, PreviewCode, MemoBadge, Note, Callout } from "~/components";


Lism CSSを利用するための手順を解説します。


## インストール

大きく分けて2通りの導入方法ができます。

### CSS設計のみを導入したい場合

シンプルなHTMLサイトの場合など、CSSファイルのみを読み込む場合はCDN経由で利用できます。


<PreviewTitle>CDN経由でスタイルシートを読み込む</PreviewTitle>
```html
<link href="https://cdn.jsdelivr.net/npm/lism-css/dist/css/all.css" rel="stylesheet" />
```

もちろん、CSSファイルをダウンロードして読み込んでもOKです。


### 専用のコンポーネントを使用したい場合

React(.jsx)、Astro(.astro)形式のコンポーネントを npmパッケージとして 配布しています。

### 1. パッケージのインストール

<Preview>
	<PreviewTitle>パッケージのインポート</PreviewTitle>
	<PreviewCode slot='tab' label='npm'>
	```bash frame="none"
	npm i lism-css
	```
	</PreviewCode>
	<PreviewCode slot='tab' label='pnpm'>
	```bash frame="none"
	pnpm add lism-css
	```
	</PreviewCode>
	<PreviewCode slot='tab' label='yarn'>
	```bash frame="none"
	yarn add lism-css
	```
	</PreviewCode>
</Preview>



{/* <Note title='最新版mainブランチのテスト'>
pkg.pr.new を導入してます。

```zsh frame="none"
pnpm add https://pkg.pr.new/lism-css/lism-css/lism-css@{commitID}
```
</Note> */}



### 2. CSSの読み込み

<PreviewTitle>グローバルスタイルとして読み込んでください。</PreviewTitle>
```js
import 'lism-css/all.css';
```

例：Next.jsであれば、`_app.js`や`layout.js`でimportしてください。


{/* ### Using Components */}
### 3. コンポーネントの読み込み

<Preview>
<PreviewTitle>各コンポーネントのインポート</PreviewTitle>
<PreviewCode slot='tab' label='.jsx'>
```jsx
import { Box, Flex, ... } from 'lism-css/react';
```
</PreviewCode>
<PreviewCode slot='tab' label='.astro'>
```jsx
import { Box, Flex, ... } from 'lism-css/astro';
```
</PreviewCode>
</Preview>



<Note title='.astroを読み込む場合の注意点' type='warning'>
`.astro`ファイルをnode_modeules内から直接読み込むことができず、追加の設定が必要な場合があります。  
"**Directory import '...' is not supported resolving ES modules** ..." というエラーが発生した場合、`astro.config.js`の`vite.ssr.noExternal`に`"lism-css"`を追加してください。

<PreviewTitle>`astro.config.js`</PreviewTitle>
```js
import { defineConfig } from "astro/config";

export default defineConfig({
  // ...other settings 
  vite: {
    ssr: {
      noExternal: ["lism-css"],
    },
  },
});
```

 
<small>（パッケージ配布方法の改善で対応できるかどうか調整中です...）</small>
</Note>



### Next.js ( for App Router)


<MemoBadge>準備中</MemoBadge>

{/* Next.js の **app ルーティング**を使用している場合、`<Accordion>`や`<Tab>`を使うには`"use client"`の記述が必要になりますが、 */}


`Accordion`など、`"use client"`の記述が必要なコンポーネントについては、`lism-css/next-js`からすでに記述済みのコンポーネントをimportできるようになっています。


```bash frame="none" filename="Add package"
import Accordion from 'lism-css/next-js/Accordion';
```


{/* ## CSSをPurgeする

ビルド環境の調整ができる場合は、[PurgeCSS](https://purgecss.com/)を利用して、必要なCSSだけを抽出することができます。<MemoBadge>未検証</MemoBadge> */}
