---
title: '-hov'
description: 'How to use the -hov prop classes for controlling hover behavior and transitions.'
---

import { Flex, Media, Layer, Spacer, Box, Center, Stack, Frame, LinkBox, Text, Inline } from 'lism-css/react';


This page introduces the CSS design for controlling hover behavior in Lism.


In Lism, hover styles are defined using Prop classes that begin with `-hov:`.
The `set--transition` class is used alongside them to configure transitions.


## Basic Usage

Hover behavior is controlled by combining a class in the format `.-hov:{prop}` with a CSS variable `--hov-{prop}`.

The Lism core package provides the following classes by default:

<EmbedCode dirPath='packages/lism-css' srcPath='src/scss/props/_hover.scss' />

(Add classes in this format as needed.)

<Spacer h='20' />

Since each built-in class defines a default value, they can be used simply by adding the class — no additional configuration required.


<Preview>
  <PreviewTitle>Usage examples of `.-hov:o`, `.-hov:c`, `.-hov:bxsh`</PreviewTitle>
  <PreviewArea p='30'>
    <LinkBox href="###" hov="o" bgc="base-2" bd p="20">
      <p>LinkBox</p>
      <p>Lorem ipsum dolor sit amet consectetur adipisicing elit.</p>
    </LinkBox>
    <LinkBox href="###" hov="c,bxsh" bd p="20">
      <p>LinkBox</p>
      <p>Lorem ipsum dolor sit amet consectetur adipisicing elit.</p>
    </LinkBox>
  </PreviewArea>
  <PreviewCode slot='tab' label='HTML'>
    ```html "-hov:o" "-hov:c" "-hov:bxsh"
    <a href="###" class="is--linkBox -hov:o -bgc:base-2 -bd -p:20">
      <p>LinkBox</p>
      <p>Lorem ipsum dolor sit amet consectetur adipisicing elit.</p>
    </a>
    <a href="###" class="is--linkBox -hov:c -hov:bxsh -bd -p:20">
      <p>LinkBox</p>
      <p>Lorem ipsum dolor sit amet consectetur adipisicing elit.</p>
    </a>
    ```
  </PreviewCode>
  <PreviewCode slot='tab' label='JSX'>
    ```jsx "hov='o'" "hov='c,bxsh'"
    <LinkBox href='###' hov='o' bgc='base-2' bd p='20'>
      <p>LinkBox</p>
      <p>Lorem ipsum dolor sit amet consectetur adipisicing elit.</p>
    </LinkBox>
    <LinkBox href='###' hov='c,bxsh' bd p='20'>
      <p>LinkBox</p>
      <p>Lorem ipsum dolor sit amet consectetur adipisicing elit.</p>
    </LinkBox>
    ```
  </PreviewCode>
</Preview>

:::info
In the [`<Lism>` component](/components/lism), passing a string to the `hov` prop automatically outputs the corresponding `-hov` class.
:::

<Spacer h='20' />


### Changing a property to an arbitrary value

<Preview>
  <PreviewTitle>Example of changing `bgc` and `c`</PreviewTitle>
  <PreviewArea p='30'>
    <LinkBox href="###" bgc="base-2" p="20" hov={{ bgc: 'brand', c: 'white' }}>
      <p>LinkBox.</p>
      <p>Lorem ipsum dolor sit amet consectetur adipisicing elit.</p>
    </LinkBox>
  </PreviewArea>
  <PreviewCode slot='tab' label='HTML'>
  ```html "-hov:bgc" "-hov:c" "--hov-bgc" "--hov-c"
  <a class="is--linkBox -hov:bgc -hov:c -bgc:base-2 -p:20" style="--hov-bgc: var(--brand); --hov-c: var(--white)" href="###">
    <p>LinkBox.</p>
    <p>Lorem ipsum dolor sit amet consectetur adipisicing elit.</p>
  </a>
  ```
  </PreviewCode>
  <PreviewCode slot='tab' label='JSX'>
  ```jsx "hov="
  <LinkBox href="###" hov={{ bgc: 'brand', c: 'white' }} bgc="base-2" p="20">
    <p>LinkBox.</p>
    <p>Lorem ipsum dolor sit amet consectetur adipisicing elit.</p>
  </LinkBox>;
  ```
  </PreviewCode>
</Preview>

:::info
In the [`<Lism>` component](/components/lism), passing an object like `hov={{ prop:value }}` outputs a `-hov:prop` class paired with a `--hov-prop:value` CSS variable.
:::

<Spacer h='20' />


<div class='u--srOnly' id='set--transition'></div>
### How to configure transitions

The [`set--transition`](/docs/css-methodology/#set--transition) class is available for this purpose.



Combine this class with CSS variables to configure transition behavior.


{/* hsl(25 50% 50% / 20%),rgb(250 50 50 / 20%)' */}
<Preview>
  <PreviewTitle>Example: combining `set--transition` to animate the bxsh intensity</PreviewTitle>
  <PreviewArea p='30'>
    <LinkBox href="###" setTransition bxsh="10" hov={{ bxsh: '40' }} bd p="20">
      <p>LinkBox</p>
      <p>Lorem ipsum dolor sit amet consectetur adipisicing elit.</p>
    </LinkBox>
  </PreviewArea>
  <PreviewCode slot='tab' label='HTML'>
    ```html "set--transition" "-bxsh:10 -hov:bxsh" "--hov-bxsh:var(--bxsh--40)"
    <a href="###" class="is--linkBox set--transition -bxsh:10 -hov:bxsh -bd -p:20" style="--hov-bxsh: var(--bxsh--40)">
      <p>LinkBox</p>
      <p>Lorem ipsum dolor sit amet consectetur adipisicing elit.</p>
    </a>
    ```
  </PreviewCode>
  <PreviewCode slot='tab' label='JSX'>
  ```jsx "setTransition" "bxsh='10' hov=\{\{ bxsh: '40'\}\}"
  <LinkBox href="###" setTransition bxsh="10" hov={{ bxsh: '40' }} bd p="20">
    <p>LinkBox</p>
    <p>Lorem ipsum dolor sit amet consectetur adipisicing elit.</p>
  </LinkBox>;
  ```
  </PreviewCode>
</Preview>


<Preview>
  <PreviewTitle>Example: slowly transitioning the border-color</PreviewTitle>
  <PreviewArea p='30'>
    <LinkBox href="###" setTransition hov={{ bdc: 'red' }} bd bdw="3px" bgc="base-2" p="20" style={{ '--hov-duration': '.5s' }}>
      <p>LinkBox</p>
      <p>Lorem ipsum dolor sit amet consectetur adipisicing elit.</p>
    </LinkBox>
  </PreviewArea>
  <PreviewCode slot='tab' label='HTML'>
    ```html "set--transition" "--hov-duration:1s"
    <a class="is--linkBox -hov:bdc set--transition -bd -bgc:base-2 -p:20" style="--hov-bdc: var(--red); --hov-duration: 0.5s; --bdw: 3px" href="###">
      <p>LinkBox</p>
      <p>Lorem ipsum dolor sit amet consectetur adipisicing elit.</p>
    </a>
    ```
  </PreviewCode>
  <PreviewCode slot='tab' label='JSX'>
    ```jsx "setTransition" "--hov-duration"
    <LinkBox
      href='###' setTransition hov={{ bdc:'red' }} bd bdw='3px' bgc='base-2' p='20'
      style={{'--hov-duration':'.5s'}}~
    >
      <p>LinkBox</p>
      <p>Lorem ipsum dolor sit amet consectetur adipisicing elit.</p>
    </LinkBox>
    ```
  </PreviewCode>
</Preview>


### Creating a custom `-hov` class

<Preview css={`
@media (any-hover: hover) {
  .-hov\\:shadowUp:hover {
    box-shadow: var(--bxsh--40);
    translate: 0 -3px;
  }
}`}>
  <PreviewTitle>Adding `.-hov:shadowUp`</PreviewTitle>
  <PreviewArea p='30'>
    <LinkBox href="###" setTransition hov="shadowUp" bxsh="10" bd p="20">
      <p>LinkBox</p>
      <p>Lorem ipsum dolor sit amet consectetur adipisicing elit.</p>
    </LinkBox>
  </PreviewArea>

  <PreviewCode slot='tab' label='Additional CSS'>
    ```scss
    @media (any-hover: hover) {
      .-hov\:shadowUp:hover {
        box-shadow: var(--bxsh--40);
        translate: 0 -3px;
      }
    }
    ```
  </PreviewCode>
  <PreviewCode slot='tab' label='HTML'>
    ```html "-hov:shadow" "-hov:up"
    <a href="###" class="is--linkBox set--transition -hov:shadow -hov:up -bd -p:20">
      <p>LinkBox</p>
      <p>Lorem ipsum dolor sit amet consectetur adipisicing elit.</p>
    </a>
    ```
  </PreviewCode>
  <PreviewCode slot='tab' label='JSX'>
    ```jsx "hov='shadowUp'"
    <LinkBox href="###" setTransition hov="shadowUp" bd p="20">
      <p>LinkBox</p>
      <p>Lorem ipsum dolor sit amet consectetur adipisicing elit.</p>
    </LinkBox>;
    ```
  </PreviewCode>
</Preview>


<div class='u--srOnly' id='set--hov'></div>
## `.set--hov` and Special Variables


Lism defines a class called [`set--hov`](/docs/css-methodology/#set--hov), which sets up the `--_isHov` and `--_notHov` variables.

- `--_isHov` is defined as an empty value only when the element is not hovered (and not focused within).
- `--_notHov` is the opposite — it is defined as an empty value when the element is hovered or focused within.

```css
@media (any-hover: hover) {
  .set--hov:hover {
    --_notHov: ;
  }
  .set--hov:not(:is(:hover, :focus-within)) {
    --_isHov: ;
  }
}
@media (any-hover: none) {
  .set--hov {
    --_isHov: ;
  }
}
.set--hov:is(:focus-visible, :focus-within) {
  --_notHov: ;
}
```

For example, writing `var(--_isHov, value)` means that since `--_isHov` is undefined on hover, `value` is applied.
When not hovered, `--_isHov` is defined as empty, making it an invalid value that is ignored. (Note: if a style is already applied to the same property on the same element, it will also be invalidated.)

Here is an example:

<Preview css={`
.-hov\\:test {
  color: var(--_isHov, green) var(--_notHov, blue);
  background-color: var(--_notHov, lightgray);
  box-shadow: var(--_isHov, var(--bxsh--30));
}
`}>
  <PreviewTitle>Adding `.-hov:test`</PreviewTitle>
  <PreviewArea p='30'>
    <div className="-p:30 -bd -fz:l set--hov -hov:test">BOX</div>
  </PreviewArea>

  <PreviewCode slot='tab' label='Additional CSS'>
    ```css
    /* Variables are defined in .set--hov */
    .-hov\:test {
      color: var(--_isHov, red) var(--_notHov, blue); /* red on hover, blue otherwise */
      background-color: var(--_notHov, lightgray); /* lightgray when not hovered, invalid on hover */
      box-shadow: var(--_isHov, var(--bxsh--30)); /* shadow on hover, invalid when not hovered */
    }
    ```
  </PreviewCode>
  <PreviewCode slot='tab' label='HTML'>
    ```html "set--hov -hov:test"
    <div class="-p:30 -bd -fz:l set--hov -hov:test">BOX</div>
    ```
  </PreviewCode>
</Preview>


In the `color` example above, two `var()` expressions are placed side by side — one is always empty while the other has a value — so only one of them applies at any given time.
This technique allows you to write both hover and non-hover styles in the same declaration.


### Passing hover state to child elements

This is where `set--hov` becomes particularly powerful.
By adding `set--hov` to a parent element and using the `--_isHov` and `--_notHov` variables in child elements, you can control the behavior of children when the parent is hovered.


The core package includes a small set of such child-targeting classes under the `-hov:to:xxx` naming convention by default.

```css
/* Hide when parent set--hov is hovered */
.-hov\:to\:hide {
  --transProp: opacity;
  opacity: var(--_isHov, 0);
}

/* Show when parent set--hov is hovered */
.-hov\:to\:show {
  --transProp: opacity, visibility;
  opacity: var(--_notHov, 0);
  visibility: var(--_notHov, hidden);
}

.-hov\:to\:zoom {
  --transProp: scale;
  scale: var(--_isHov, 1.1);
}
```


<Preview>
  <PreviewTitle>Example using `to:show`</PreviewTitle>
  <PreviewArea p='20'>
    <Frame isLinkBox setHov href="#banner-link" ar="16/9" bdrs="30">
      <Media isLayer src="https://cdn.lism-css.com/img/a-1.jpg" alt="" width="960" height="640" loading="lazy" />
      <Layer setTransition hov="to:show" bgc="rgb(0 0 0 / 40%)" style={{ backdropFilter: 'blur(6px)' }}>
        <Center h="100%" c="#fff">
          <Inline fz="2xl" fs="italic" fw="light" lts="l" bdrs="99" px="20" py="10">
            View More
          </Inline>
        </Center>
      </Layer>
    </Frame>
  </PreviewArea>
  <PreviewCode slot='tab' label='HTML'>
    ```html "set--hov" "-hov:to:show" "set--transition"
    <a class="l--frame is--linkBox set--hov -ar:16/9 -bdrs:30" href="#banner-link">
      <img class="is--layer" src="https://cdn.lism-css.com/img/a-1.jpg" width="960" height="640" loading="lazy" />
      <div class="is--layer set--transition -hov:to:show -bgc" style="--bgc: rgb(0 0 0 / 10%); backdrop-filter: blur(6px)">
        <div class="l--center -h:100% -c" style="--c: #fff">
          <span class="-fz:2xl -fs:italic -fw:light -lts:l -bdrs:99 -px:20 -py:10">View More</span>
        </div>
      </div>
    </a>
    ```
  </PreviewCode>
  <PreviewCode slot='tab' label='JSX'>
    ```jsx "setHov" "hov='to:show'" "setTransition"
    <Frame isLinkBox setHov href="#banner-link" ar="16/9" bdrs="30">
      <Media isLayer src="https://cdn.lism-css.com/img/a-1.jpg" alt="" width="960" height="640" loading="lazy" />
      <Layer setTransition hov="to:show" bgc="rgb(0 0 0 / 40%)" style={{ backdropFilter: 'blur(6px)' }}>
        <Center h="100%" c="#fff">
          <Inline fz="2xl" fs="italic" fw="light" lts="l" bdrs="99" px="20" py="10">
            View More
          </Inline>
        </Center>
      </Layer>
    </Frame>;
    ```
  </PreviewCode>
</Preview>



### Creating custom `-hov:to` classes


Let's create a custom `-hov:to:zoom` class that zooms in when the parent is hovered, and a `-hov:to:letterSpread` class that spreads the letter spacing.

Additionally, we'll override `--hov-duration` on the parent to change the transition duration of all child elements at once.

<Preview css={`
.-hov\\:to\\:letterSpread {
  --transProp: letter-spacing;
  letter-spacing: var(--_isHov, 4px);
}`}>
  <PreviewTitle>Example: adjusting child hover actions with custom classes</PreviewTitle>
  <PreviewArea p='20'>
    <LinkBox href="#banner-link" setHov ov="hidden" ar="16/9" bdrs="30" style={{ '--hov-duration': '.4s' }}>
      <Frame isLayer>
        <Media setTransition hov="to:zoom" src="https://cdn.lism-css.com/img/a-2.jpg" alt="" width="960" height="640" loading="lazy" />
      </Frame>
      <Layer setTransition hov="to:hide" style={{ backdropFilter: 'grayscale(70%)' }} />
      <Center isLayer c="#fff" bgc="rgb(0 0 0 / 40%)">
        <Text setTransition hov="to:letterSpread" fz="2xl">
          LinkBox
        </Text>
      </Center>
    </LinkBox>
  </PreviewArea>

  <PreviewCode slot='tab' label='Additional CSS'>
  ```css
  .-hov\:to\:letterSpread {
    --transProp: letter-spacing;
    letter-spacing: var(--_isHov, 0.125em);
  }
  ```
  </PreviewCode>
  <PreviewCode slot='tab' label='HTML'>
    ```html "set--hov" "-hov:to:zoom" "-hov:to:letterSpread" "--hov-duration"
    <a href="#banner-link" class="is--linkBox set--hov -ar:16/9 -ov:hidden -bdrs:30" style="--hov-duration: 0.4s">
      <div class="l--frame is--layer">
        <img class="is--layer set--transition -hov:to:zoom" src="https://cdn.lism-css.com/img/a-2.jpg" width="960" height="640" loading="lazy" />
      </div>
      <div class="is--layer set--transition -hov:to:hide" style="backdrop-filter: grayscale(70%)"></div>
      <div class="l--center is--layer -c -bgc" style="--c: #fff; --bgc: rgb(0 0 0 / 40%)">
        <span class="-hov:to:letterSpread -fz:2xl">LinkBox</span>
      </div>
    </a>
    ```
  </PreviewCode>
  <PreviewCode slot='tab' label='JSX'>
    ```jsx "setHov" "hov='to:zoom'" "hov='to:letterSpread'" "--hov-duration"
    <LinkBox href="#banner-link" setHov ov="hidden" ar="16/9" bdrs="30" style={{ '--hov-duration': '.4s' }}>
      <Frame isLayer>
        <Media setTransition hov="to:zoom" src="https://cdn.lism-css.com/img/a-2.jpg" alt="" width="960" height="640" loading="lazy" />
      </Frame>
      <Layer setTransition hov="to:hide" style={{ backdropFilter: 'grayscale(70%)' }} />
      <Center isLayer c="#fff" bgc="rgb(0 0 0 / 40%)">
        <Text setTransition hov="to:letterSpread" fz="2xl">
          LinkBox
        </Text>
      </Center>
    </LinkBox>;
    ```
  </PreviewCode>
</Preview>
