---
title: 'Group'
description: 'A Lism component that outputs a <div> tag.'
order: 4
---
import { Group } from "lism-css/astro";
import { DummyText } from '@lism-css/ui/astro';

`<Group>` is a Lism component that outputs a `<div>` tag.  
Use it as a general-purpose container for grouping elements.

:::info
`<Group>` is an alias for `<Lism as="div">`. All [Lism Props](/en/docs/core-components/Lism/) are available.  
You can change the output tag with the `as` prop: `div`(default) / `section` / `article` / `figure` / `nav` / `aside` / `header` / `footer` / `main` / `fieldset` / `hgroup`
:::

:::note[Difference from `<Lism>`]
`<Lism>` also outputs a `<div>` by default, but using `<Group>` makes the intent — "this is a grouping container" — explicit in your code.
:::


## Import

<ImportPackage component='Group' />


## Basic Usage

<Preview>
  <PreviewTitle>`<Group>` output example</PreviewTitle>
  <PreviewArea p='20'>
    <Group p="20" bgc="base-2" bdrs="20">
      <DummyText />
    </Group>
  </PreviewArea>
  <PreviewCode slot='tab' label='JSX'>
    ```jsx
    <Group p="20" bgc="base-2" bdrs="20">
      Lorem ipsum text...
    </Group>
    ```
  </PreviewCode>
  <PreviewCode slot='tab' label='HTML'>
    ```html
    <div class="-p:20 -bgc:base-2 -bdrs:20">Lorem ipsum text...</div>
    ```
  </PreviewCode>
</Preview>
