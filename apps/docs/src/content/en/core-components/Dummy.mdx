---
title: 'Dummy'
description: 'A Lism component for outputting dummy text.'
---
import { Lism, HTML, Dummy, Stack } from "lism-css/astro";

A Lism component for outputting dummy text.


## Props

| Property | Description |
|---|---|
| `lang` | Specifies the language. Defaults to `en`. Also supports `ja` and `ar`. |
| `length` | Specifies the length of the dummy text. Valid values are `xs`, `s`, `m`, `l`, `xl`, `codes`. Defaults to `m`. |
| `pre` | Specifies text to display before the dummy text. |
| `as` | Specifies the HTML tag for the dummy text. |
| `offset` | Specifies an offset for the dummy text. Text is trimmed at `,` and `.` boundaries, so the resulting text will be shorter. |

## Import

<ImportPackage component='Dummy' />


## Dummy Text Examples

<Preview>
	<PreviewTitle>Dummy text in different languages</PreviewTitle>
	<PreviewArea p='20'>
		<Dummy />
		<Dummy lang='ja' />
	</PreviewArea>
	<PreviewCode>
		```jsx
		<Dummy />
		<Dummy lang='ja' />
		```
	</PreviewCode>
</Preview>

<Preview>
	<PreviewTitle>Dummy text at different lengths</PreviewTitle>
	<PreviewArea p='20'>
		<Stack g='5'>
			<Dummy lang='ja' length='xs' pre='xs: ' />
			<Dummy length='xs' pre='xs: ' />
		</Stack>
		<Stack g='5'>
		<Dummy lang='ja' length='s' pre='s: ' />
		<Dummy length='s' pre='s: ' />
		</Stack>
		<Stack g='5'>
			<Dummy lang='ja' length='m' pre='m: ' />
			<Dummy length='m' pre='m: ' />
		</Stack>
		<Stack g='5'>
			<Dummy lang='ja' length='l' pre='l: ' />
			<Dummy length='l' pre='l: ' />
		</Stack>
		<Stack g='5'>
			<Dummy lang='ja' length='xl' pre='xl: ' />
			<Dummy length='xl' pre='xl: ' />
		</Stack>
	</PreviewArea>
	<PreviewCode>
		```jsx
		<Dummy lang='ja' length='xs' pre='xs: ' />
		<Dummy lang='ja' length='s' pre='s: ' />
		<Dummy lang='ja' length='m' pre='m: ' />
		<Dummy lang='ja' length='l' pre='l: ' />
		<Dummy lang='ja' length='xl' pre='xl: ' />
		```
	</PreviewCode>
</Preview>

<Preview>
	<PreviewTitle>Setting `length='codes'` outputs dummy text containing `<b>`, `<i>`, `<a>`, and `<code>` elements.</PreviewTitle>
	<PreviewArea p='20'>
		<Dummy lang='ja' length='codes' />
	</PreviewArea>
	<PreviewCode>
		```jsx
		<Dummy lang='ja' length='codes' />
		```
	</PreviewCode>
</Preview>


<Preview>
	<PreviewTitle>Example of offset</PreviewTitle>
	<PreviewArea p='20'>
		<Dummy lang='ja' length='xs' />
		<Dummy lang='ja' offset={1} />
		<Dummy lang='ja' length='l' offset={3} />
	</PreviewArea>
	<PreviewCode>
		```jsx
		<Dummy lang='ja' length='xs' />
		<Dummy lang='ja' offset={1} />
		<Dummy lang='ja' length='l' offset={3} />
		```
	</PreviewCode>
</Preview>


## Dummy Lists

When `as='ul'` or `as='ol'` is specified, a list is rendered.
<Preview>
	<PreviewTitle>Dummy list example 1</PreviewTitle>
	<PreviewArea p='20'>
		<Dummy as='ul' />
		<Dummy as='ol' />
	</PreviewArea>
	<PreviewCode>
		```jsx
		<Dummy as='ul' />
		<Dummy as='ol' />
		```
	</PreviewCode>
</Preview>


## Dummy Image

When `as='img'` is specified, a dummy image is fetched from `https://cdn.lism-css.com/`.
<Preview>
	<PreviewTitle>Example</PreviewTitle>
	<PreviewArea p='20'>
		<Dummy as='img' />
	</PreviewArea>
	<PreviewCode>
		```jsx
		<Dummy as='img' />
		```
	</PreviewCode>
</Preview>
