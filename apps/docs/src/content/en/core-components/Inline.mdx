---
title: 'Inline'
description: 'A Lism component that outputs a <span> tag.'
order: 3
---
import { Text, Inline } from "lism-css/astro";

`<Inline>` is a Lism component that outputs a `<span>` tag.  
Use it when you want to apply styles to a portion of text.

:::info
`<Inline>` is an alias for `<Lism as="span">`. All [Lism Props](/docs/core-components/Lism/) are available.  
You can change the output tag with the `as` prop: `span`(default) / `em` / `strong` / `small` / `code` / `time` / `i` / `b` / `mark` / `abbr` / `cite` / `kbd`
:::

## Import

<ImportPackage component='Inline' />


## Basic Usage

<Preview>
  <PreviewTitle>`<Inline>` output example</PreviewTitle>
  <PreviewArea p='20'>
    <Text>
      <Fragment>You can style </Fragment>
      <Inline fz="l" fs="italic">inline elements</Inline>
      <Fragment> within text.</Fragment>
    </Text>
  </PreviewArea>
  <PreviewCode slot='tab' label='JSX'>
    ```jsx
    <Text>
      You can style
      <Inline fz="l" fs="italic">
        inline elements
      </Inline>
      within text.
    </Text>
    ```
  </PreviewCode>
</Preview>


<Preview>
  <PreviewTitle>`as="strong"` example</PreviewTitle>
  <PreviewArea p='20'>
    <Text>
      This is{' '}
      <Inline as="strong" c="red">emphasized</Inline>{' '}
      text.
    </Text>
  </PreviewArea>
  <PreviewCode slot='tab' label='JSX'>
    ```jsx
    <Text>
      This is{' '}
      <Inline as="strong" c="red">
        emphasized
      </Inline>{' '}
      text.
    </Text>
    ```
  </PreviewCode>
  <PreviewCode slot='tab' label='HTML'>
    ```html
    <p>This is <strong class="-c" style="--c: var(--red)">emphasized</strong> text.</p>
    ```
  </PreviewCode>
</Preview>
