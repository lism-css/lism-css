---
title: 'CSS Methodology'
description: 'An overview of the CSS layer structure and class design methodology behind Lism CSS.'
---
import { Lism, Box } from "lism-css/astro";

export const WhsNw = ({children}) => {
  return <span className='-whs:nowrap'>{children}</span>
}

This page covers the CSS design principles behind Lism.


{/* Key points are as follows.

- Predefined CSS layer structure (prevents specificity conflicts)
- Reusable design tokens (improves maintainability and consistency)
- Granular, purpose-specific classes (improves readability and reusability) */}
{/* - Inlining rarely used CSS is acceptable */}



## Lism CSS Layer Structure

To address the common problem of CSS specificity becoming overly complex, Lism defines a clear layer structure.

<table className="l--table">
  <thead>
    <tr>
      <th>Layer</th>
      <th>Role</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Settings</td>
      <td>SASS configuration variables, mixins, and other constructs that do not directly output styles.</td>
    </tr>
    <tr>
      <td>Base<br/>`lism-base`</td>
      <td>The layer for Reset CSS, token definitions, variable sets, and initial element styles.<br/>`set--{state}` classes are also defined here.</td>
    </tr>
    <tr>
      <td class='-whs:nowrap'>Trait<br/>`lism-trait`</td>
      <td>The layer for classes that declaratively attach a role or feature to an element (`is--{name}`, `has--{name}`).</td>
    </tr>
    <tr>
      <td class='-whs:nowrap'>Primitives<br/>`lism-primitive`</td>
      <td>
        The layer for Primitives — the building blocks for composing layouts.<br />
        `l--{layout}` and `a--{atomic}` are defined here.
      </td>
    </tr>
    <tr>
      <td class='-whs:nowrap'>Block<br/>`lism-block`</td>
      <td>The layer where you define base styles for common, reusable site-wide parts (`b--{name}`) inside `@layer lism-block`. The core only declares the layer order and ships no styles of its own here.</td>
    </tr>
    <tr>
      <td class='-whs:nowrap'>Custom<br/>`lism-custom`</td>
      <td>A customization layer for overriding existing Lism classes, or for placing custom classes (`c--{name}`, prefix-free classes, etc.).</td>
    </tr>
    <tr>
      <td class='-whs:nowrap'>Utility<br/>`lism-utility`</td>
      <td>The layer for utility classes with a clear purpose or style (`u--{name}`).</td>
    </tr>
    <tr>
      <td class='-whs:nowrap'>Props<br/>`-`</td>
      <td>**Property Classes** (`-{prop}:{val}`) tied to individual CSS properties.<br />This group is the only one placed outside of layers, giving it higher specificity while still allowing reasonable coexistence with external CSS.</td>
    </tr>
  </tbody>
</table>


## Design Tokens

CSS custom properties are defined for key CSS properties — typography, spacing, color, shadows, and more — using stepped preset values and semantic keywords.

Using tokens brings **consistency** to the design, makes the **intent** behind property usage more readable, and improves **maintainability** by allowing site-wide adjustments to be made simply by updating token values.


See the [Tokens reference page](/en/docs/tokens/) for details.
<DocsLink path="/docs/tokens/" />



## CSS Class Design

This section explains the design rationale behind the core class groups in Lism CSS.

### Set Class

**Set Classes** are utility classes defined within the `lism-base` layer. They are used for HTML element base styling or for setting variables.

| Category | Format | Example |
|---|---|---|
|Set Class|`set--{style}`|`set--plain`,<br />`set--hov`,<br />`set--bxsh`|

### Primitive Class

**Primitive Classes** are small building blocks for composing layouts.

These are further categorized into several types:

| Category | Format | Example |
|---|---|---|
|Layout Primitives<br /><small>Primitives that serve as layout building blocks</small>|`l--{name}`|`l--grid`,<br />`l--columns`|
|Atomic Primitives<br /><small>The smallest layout unit primitives</small>|`a--{name}`|`a--divider`,<br />`a--icon`|

Primitive classes are standalone — they have no variations or child element classes. Only [Block Class](#block-class) (`b--`) and [Custom Class](#custom-class) (`c--`), described later, follow a BEM structure.

Combining Primitives follows these rules:

| Combination | Allowed | Notes |
|---|---|---|
| `l--` × `l--` | Not allowed | Same category cannot be combined |
| `a--` × `a--` | Not allowed | Same category cannot be combined |
| `l--` × `a--` | Discouraged | Not intended to coexist |

### Trait Class

**Trait Classes** declare a role on an element or attach a feature to it.

| Category | Format | Example |
|---|---|---|
|`is--` Trait<br /><small>Declares a role ("is a …") on an element</small>|`is--{name}`|`is--container`,<br />`is--wrapper`,<br />`is--layer`,<br />`is--boxLink`|
|`has--` Trait<br /><small>Attaches a feature ("has a …") to an element</small>|`has--{name}`|`has--transition`,<br />`has--gutter`,<br />`has--snap`|

The criteria for choosing between `is--` and `has--` are:

|  | `is--` | `has--` |
|---|---|---|
| Meaning | "is a …" (role / existence declaration) | "has a …" (feature attachment) |
| CSS variables | Not required | Required (provides customization points) |

See the [naming guide](/en/docs/naming/) for the detailed decision criteria and usage guidelines.


### Block Class

**Block Classes** prefixed with `b--` are for common, reusable, site-wide base parts (button, badge, card-level components).  
The name guarantees that "the base style lives in CSS" — base styles are defined in `@layer lism-block` and managed in a CSS file. The core `lism-css` only provides the dedicated layer; you define the classes themselves.

```css
@layer lism-block {
  .b--btn { /* ... */ }
}
```

Use `b--` only for parts that satisfy all three of the following:

1. It's a common part reused across multiple pages or multiple places on the site.
2. It's a part where a single class should define nearly the entire base style (you want style changes to stay contained to the CSS file).
3. Its granularity is a self-contained, button/badge/card-level part (not a full page section or the site skeleton).

Parts that don't meet these criteria should be `c--`. You can use it purely as a name that shows what part it is, without writing any CSS (see [Categories of custom classes](#categories-of-custom-classes) below).

`b--` classes can have a BEM structure (Block / Modifier / Element), each defined in the following format:

| Category | Format | Example |
|---|---|---|
|Block<br /><small>The base class</small>|`b--{name}`|`b--btn`,<br />`b--card`|
|Modifier<br /><small>Variation</small>|`b--{name}--{modifier}`|`b--btn--outline`|
|Element<br /><small>Child element</small>|`b--{name}_{element}`|`b--card_header`,<br />`b--card_body`|

:::point
Modifiers are used alongside the Block class, e.g. `.b--btn.b--btn--outline`.  
Elements use a single underscore (`_`) as the separator.
:::

Combining Blocks (`.b--xxx.b--yyy`) is generally not allowed, but the following combinations are allowed:

- Block with its own Modifier: `.b--xxx.b--xxx--variant`
- Block with another Block's Element: `.b--xxx.b--yyy_elem`

:::point
You can combine `b--` with Property Classes. Property Classes sit outside any layer and always take precedence over `b--`, so we recommend using them for exceptions and breakpoint switches (e.g. `-p_sm`).  
Conversely, `lism-block` is a stronger layer than `lism-primitive`, so adding an `l--` or Trait class to a `b--` element cannot override properties the `b--` side already sets. If you need a layout variation, define a Modifier class in CSS, or override it with a Property Class.
:::



### Utility Class

Utility classes with a clear decorative or functional purpose are defined as follows:

| Category | Format | Example |
|---|---|---|
|Utility Class|`u--{style}`|`u--cbox`,<br/>`u--trim`|



### Property Class

In Lism CSS, utility classes that correspond to a single CSS property are defined as **Property Classes**.

Utility classes are provided for key properties, covering commonly used values and dedicated token values, with responsive support built in.


Property Classes come in three formats:

| Format | Description | Example |
|---|---|---|
|<WhsNw>`-{prop}:{value}`</WhsNw>| Utility classes for applying the primary values or token values for each property |<WhsNw>`-fz:l`</WhsNw>,<br/><WhsNw>`-d:none`</WhsNw>|
|`-{prop}`| A class that receives the `--{prop}` variable |`-p`,<br/>`-fz`|
|`-{prop}_{bp}`| A class that receives the `--{prop}_{bp}` variable |<WhsNw>`-p_sm`</WhsNw>,<br/>`-p_md`|


:::warning
\* Classes for all CSS properties are not available by default.
\* Breakpoint-responsive classes are also only available by default for a select subset of key properties.
:::



See the [Property Class reference page](/en/docs/property-class) for the full list.

<DocsLink path="/docs/property-class" />


### Responsive Property Classes

The `-{prop}_{bp}` class format combined with the `--{prop}_{bp}` variable format enables responsive styling.

<Preview>
  <PreviewTitle>Practical usage example</PreviewTitle>
  <PreviewArea resize>
    <Lism p={[20, 40, 50]} bd>BOX</Lism>
  </PreviewArea>
  <PreviewCode label="HTML" slot="tab">
```html "-p_sm" "-p_md" "--p_sm" "--p_md"
<div class="-p:20 -p_sm -p_md -bd" style="--p_sm: var(--s40); --p_md: var(--s50)">BOX</div>
```
  </PreviewCode>
  <PreviewCode label="JSX" slot="tab">
```jsx
<Box p={[20, 40, 50]} bd>
  BOX
</Box>
```
  </PreviewCode>
</Preview>

See the [Responsive styling guide](/en/docs/responsive/) for details.
<DocsLink path="/docs/responsive/" />



## Naming and placing custom classes

When you add your own classes that match one of Lism CSS's existing class categories (`set--`, `is--`, `has--`, `l--`, `a--`, `u--`, `-{prop}:{value}`), use the same prefix and add them to the corresponding definition layer.

If you need to override an existing Lism class, do so within that same layer.

`set--` overrides belong in `@layer lism-base`. Place them after the Lism `main.css` is loaded, or boost the specificity by repeating the class name (e.g. `.set--xxx.set--xxx`) to ensure the override takes effect.

```css
@layer lism-base {
  /* New addition */
  .set--hoge { /* ... */ }

  /* Override */
  .set--plain.set--plain { /* ... */ }
}
```


### Categories of custom classes

Classes you define yourself that don't fit any of Lism CSS's built-in categories should be organized into the following two categories.

| Category | Naming | Where to write styles |
|---|---|---|
|Common, reusable site-wide parts<br /><small>Button, badge, card-level components</small>|`b--{name}`<br /><small>e.g. `b--btn`</small>|Define base styles in `@layer lism-block` and manage them in CSS. (→ [Block Class](#block-class))|
|All other custom classes<br /><small>Components, zoning, page-specific elements — any granularity</small>|`c--{name}`<br /><small>e.g. `c--card`,<br />`c--siteHeader`</small>|Compose mainly with Property Classes. You can use it purely as a name that shows what part it is, without writing any CSS. If you write CSS, define it in `@layer lism-custom`. (→ [Custom Class](#custom-class))|

Use `b--` only for parts that satisfy all three [Block Class](#block-class) conditions (reused site-wide, base style managed in CSS, button/badge/card-level granularity); everything else is `c--`. The name `b--` guarantees that "the base style lives in CSS"; `c--` carries no such guarantee. The site skeleton and page-specific elements stay `c--` even when you write CSS for them. When in doubt, start with `c--` and rename it to `b--` once the part meets all three conditions and you want to switch to CSS-managed styling.

Free-form naming without a prefix is still available (see [Rules for prefix-free classes](#rules-for-prefix-free-classes) below). If you want to keep things consistent with Lism's conventions, `c--` is recommended.

```css
@layer lism-block {
  .b--btn { /* ... */ }
}

@layer lism-custom {
  .c--siteHeader { /* ... */ }
  .c--card { /* ... */ }
  .frontHero { /* ... */ }
}
```

### Custom Class

**Custom Classes** prefixed with `c--` are custom classes you're free to define beyond the core's built-in classes.  
The name corresponds to the `lism-custom` layer, and you can use it for any granularity — components, zoning, page-specific elements, and more.

The basic approach is to compose their style mainly through Property Classes; you can use a `c--` class purely as a name that shows what part it is, without writing any CSS. When a Property Class can't cover a more complex style, define the CSS in `@layer lism-custom`.

```css
@layer lism-custom {
  .c--card { /* ... */ }
  .c--pricing { /* ... */ }
}
```

`c--` classes can follow the same BEM notation and combination rules as [Block Class](#block-class), giving them a BEM structure (Block / Modifier / Element):

| Category | Format | Example |
|---|---|---|
|Block<br /><small>The base class</small>|`c--{name}`|`c--button`,<br />`c--card`|
|Modifier<br /><small>Variation</small>|`c--{name}--{modifier}`|`c--button--outline`|
|Element<br /><small>Child element</small>|`c--{name}_{element}`|`c--card_header`,<br />`c--card_body`|

### Rules for prefix-free classes

- Avoid a single generic noun (`.hero`, `.card`, etc.); use camelCase that includes the page's slug or similar (`.frontHero`). This prevents collisions with external CSS and makes the class's origin clear from its name alone.
- Place the CSS in `@layer lism-custom`. Writing it outside a layer breaks the assumption that Property Classes can override it.
- Once you start reusing it across multiple pages, promote it to `c--` (or to `b--` if it meets the [Block Class](#block-class) conditions).


## Using data attributes for dynamic state

For state management that is dynamically toggled by JavaScript, use data attributes.

Example: `[data-opened]`, `[data-active]`

