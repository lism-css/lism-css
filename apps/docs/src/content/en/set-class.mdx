---
title: 'Set Class'
description: 'Learn about the set-- utility classes for overriding base styles and redefining tokens.'
---
import { Lism, Box, Media, Flex, Stack, Spacer, Frame, Text } from 'lism-css/astro';
import { Button } from '@lism-css/ui/astro';


Utility classes defined in the `@lism-base` layer are provided in the format `set--{className}`. They are used to override base styles, redefine tokens, and set initial values for functional properties.


### `set--plain`

A class for resetting primary styles.

<SrcCode path="src/scss/base/set/_plain.scss" />

<Preview>
  <PreviewTitle>Applying `set--plain` to a `button` element</PreviewTitle>
  <PreviewArea p="20" layout="">
    <div class="l--flex -g:20 -ff:mono">
      <button class="set--plain">button</button>
    </div>
  </PreviewArea>
  <PreviewCode slot="tab" label="HTML">
  ```html "'set--plain'"
  <div class="l--flex -g:20 -ff:mono">
    <button class="set--plain">button</button>
  </div>
  ```
  </PreviewCode>
</Preview>



### `set--shadow`

Elements with the `.set--shadow` class have the `--bxsh--` variables that compose the shadow redefined.

<SrcCode path="src/scss/base/tokens/_shadow.scss" />

By overriding the `--shc` variable here, you can adjust the color tint of the shadow.

<Preview>
  <PreviewTitle>Example of overriding shadow color</PreviewTitle>
  <PreviewArea p="30" nogrid>
    <Stack g="10">
      <Text> Default: </Text>
      <Flex className="set--shadow -g:30" px-s="15">
        <Box bxsh="20" p="30" bdrs="20">
          <Fragment>Box</Fragment>
        </Box>
        <Box bxsh="40" p="30" bdrs="20">
          <Fragment>Box</Fragment>
        </Box>
      </Flex>
      <Spacer h="40" />
      <Text> Overriding variables with set--shadow: </Text>
      <Flex className="set--shadow -g:30" px-s="15" style={{ '--shc': 'hsl(200 50% 50% / 20%)' }}>
        <Box bxsh="20" p="30" bdrs="20">
          <Fragment>Box</Fragment>
        </Box>
        <Box bxsh="40" p="30" bdrs="20">
          <Fragment>Box</Fragment>
        </Box>
      </Flex>
    </Stack>
    <div class="a--spacer" />
  </PreviewArea>
    <PreviewCode slot="tab" label="HTML">
    ```html "set--shadow"
    <div class="l--flex set--shadow -g:30" style="--shc: hsl(200 50% 50% / 20%)">
      <div class="l--box -bxsh:20 -p:30 -bdrs:20">Box</div>
      <div class="l--box -bxsh:40 -p:30 -bdrs:20">Box</div>
    </div>
    ```
  </PreviewCode>
  <PreviewCode slot="tab" label="JSX">
    ```jsx "set--shadow"
    <Flex className="set--shadow" g="30" style={{ '--shc': 'hsl(200 50% 50% / 20%)' }}>
      <Box bxsh="20" p="30" bdrs="20">
        Box
      </Box>
      <Box bxsh="40" p="30" bdrs="20">
        Box
      </Box>
    </Flex>
    ```
  </PreviewCode>
</Preview>


### `set--hov`

Sets the hover state variables (`--_notHov`, `--_isHov``) as follows. Primarily used to **control hover styles on child elements**.

- `--_isHov` is defined as an empty value only when not hovered (and not focus-within).
- Conversely, `--_notHov` is defined as an empty value when hovered or focus-within.

<SrcCode path="src/scss/base/set/_hov.scss" />


This empty variable trick enables the following patterns:

- `var(--_isHov, {value to apply on hover})`
- `var(--_notHov, {value to apply when not hovered})`

For example, with `color: var(--_isHov, red)`, when hovered `--_isHov` becomes undefined, so `color: red` is applied.

:::info
Using `var(--_isHov, ...) var(--_notHov, ...)` allows you to **write both hover and non-hover styles on a single line**, but this does not work for properties like `box-shadow` that accept space-separated values.
:::

<PreviewTitle>Example: `-hov:test`</PreviewTitle>
```css
.-hov\:test {
  color: var(--_isHov, green) var(--_notHov, red); /* green on hover, red when not hovered */
  box-shadow: var(--_isHov, var(--bxsh--30)); /* shadow on hover (invalid value when not hovered) */
  opacity: var(--_notHov, var(--o---20)); /* reduced opacity when not hovered (invalid value on hover) */
}
```

<Preview css={`
.-hov\\:test {
  color: var(--_isHov, green) var(--_notHov, red);
  box-shadow: var(--_isHov, var(--bxsh--30));
  opacity: var(--_notHov, var(--o---20));
}
`}>
  <PreviewTitle>Preview of `-hov:test`</PreviewTitle>
  <PreviewArea p="30">
    <div className="set--hov -hov:test -p:30 -bd -bdc:current ">BOX</div>
  </PreviewArea>
  <PreviewCode>
    ```html "set--hov -hov:test"
    <div className="set--hov -hov:test -p:30 -bd -bdc:current ">BOX</div>
    ```
  </PreviewCode>
</Preview>


By applying `set--hov` to a parent element and using the `--_isHov` and `--_notHov` variables on child elements, you can trigger style changes on children based on the parent's hover state.

For example, this is useful for zooming an image inside a card when the parent box is hovered. The `-hov:to:zoom` class is provided out of the box for this purpose.

```css
.-hov\:to\:zoom {
  --transProp: scale;
  scale: var(--_isHov, 1.1);
}
```

<Preview>
  <PreviewTitle>Usage example of `-hov:to:zoom`</PreviewTitle>
  <PreviewArea p="30">
    <a href="###" class="l--frame is--boxLink set--hov -ar:21/9 -ov:hidden">
      <img class="has--transition -hov:to:zoom" src="https://cdn.lism-css.com/img/a-2.jpg" width="960" height="640" loading="lazy" />
      <div class="is--layer -bgc" style="--c: #fff; --bgc: rgb(0 0 0 / 50%)"></div>
      <div class="l--center is--layer -c" style="--c: #fff;">
        <span class="-fz:xl">Banner Link</span>
      </div>
    </a>
  </PreviewArea>
  <PreviewCode>
    ```html "set--hov" "-hov:to:zoom"
    <a href="###" class="l--frame is--boxLink set--hov -ar:21/9 -ov:hidden">
      <img class="has--transition -hov:to:zoom" src="https://cdn.lism-css.com/img/a-2.jpg" width="960" height="640" loading="lazy" />
      <div class="is--layer -bgc" style="--c: #fff; --bgc: rgb(0 0 0 / 50%)"></div>
      <div class="l--center is--layer -c" style="--c: #fff;">
        <span class="-fz:xl">Banner Link</span>
      </div>
    </a>
    ```
  </PreviewCode>
</Preview>



See the [`hover` documentation page](/en/docs/property-class/hov#set--hov) for usage examples.

<DocsLink path="/docs/property-class/hov#set--hov" />


### `set--innerRs`

A class that automatically calculates the inner border radius (`--bdrs--inner`) from the parent element's border radius (`--bdrs`) and padding (`--p`).
Apply `set--innerRs` to the parent element, then use `-bdrs:inner` on the child element to apply the calculated `--bdrs--inner`.

(Both `bdrs` and `p` must be specified on the parent element.)

<SrcCode path="src/scss/base/set/_innerRs.scss" />

<Preview>
  <PreviewTitle>Usage example</PreviewTitle>
  <PreviewArea p="30">
    <Lism set="innerRs" p="15" bdrs="40" bd bxsh="20">
      <Frame bdrs="inner" ar="og">
        <img src="https://cdn.lism-css.com/img/u-5.jpg" width="1920" height="1280" alt="" />
      </Frame>
    </Lism>
  </PreviewArea>
  <PreviewCode label="HTML" slot="tab">
    ```html "set--innerRs" "-bdrs:inner" "-p:15 -bdrs:40"
    <div class="set--innerRs -p:15 -bdrs:40 -bd -bxsh:20">
      <div class="l--frame -bdrs:inner -ar:og">
        <img src="https://cdn.lism-css.com/img/u-5.jpg" width="1920" height="1280" alt="" />
      </div>
    </div>
    ```
  </PreviewCode>
  <PreviewCode label="JSX" slot="tab">
    ```jsx 'set="innerRs"' 'bdrs="inner"' 'p="15" bdrs="40"'
    <Lism set="innerRs" p="15" bdrs="40" bd bxsh="20">
      <Frame bdrs="inner" ar="og">
        <img src="https://cdn.lism-css.com/img/u-5.jpg" width="1920" height="1280" alt="" />
      </Frame>
    </Lism>
    ```
  </PreviewCode>
</Preview>

<Spacer h="1rem" />

Since `p` and `bdrs` are managed as variables even when their values change at breakpoints, `set--innerRs` also supports responsive border radius.

<Preview>
  <PreviewTitle>Usage example</PreviewTitle>
  <PreviewArea p="20" resize>
    <Lism p={[10, 15]} bd bdrs={['30', '40']} class="set--innerRs">
      <Lism bd p="20" bdrs="inner" bgc="base-2">
        <Fragment>Example</Fragment>
      </Lism>
    </Lism>
  </PreviewArea>
  {/* <PreviewCode label="HTML" slot="tab">
  </PreviewCode> */}
  <PreviewCode label="JSX" slot="tab">
```jsx
<Lism p={[10, 15]} bd bdrs={['30', '40']} class="set--innerRs">
  <Lism bd p="20" bdrs="inner" bgc="base-2">
    Example
  </Lism>
</Lism>
```
  </PreviewCode>
</Preview>


{/* 
### `set--mask`

Combines with the `--mask` variable to apply a mask to the element itself.

<SrcCode path="src/scss/base/set/_mask.scss" />

In Lism components, use `set="mask"` with `css={{ '--maskImg': getSvgUrl(svgString) }}`.

<Preview>
  <PreviewTitle>Example</PreviewTitle>
  <PreviewArea p="10">
    <Media
      src="https://cdn.lism-css.com/img/u-2.jpg"
      width="960"
      height="640"
      alt=""
      loading="lazy"
      ar="og"
      set="mask"
      css={{ '--maskImg': `url('data:image/svg+xml,<svg viewBox="0 0 200 200" xmlns="http://www.w3.org/2000/svg"><path fill="%23000000" d="M170.4,25.7c16.1,14.2,23.2,38.5,24.8,61.7c1.7,23.3-1.8,45.5-12.9,62.3c-11,16.9-29.3,28.6-48.8,34.5 c-19.4,5.9-40,6.1-59.5,0.4c-19.5-5.6-37.9-17.4-51.7-34.9c-14-17.5-23.2-40.8-18-60.2c5-19.4,24.5-34.7,43.5-48.4 s37.6-25.7,59.5-29.7C129.1,7.3,154.2,11.4,170.4,25.7z" /></svg>')` }}
    />
  </PreviewArea>
  <PreviewCode slot="tab" label="JSX">
    ```jsx
    <Media
      src="https://cdn.lism-css.com/img/u-2.jpg"
      width="960"
      height="640"
      alt=""
      loading="lazy"
      ar="og"
      set="mask"
      css={{ '--maskImg': getSvgUrl(`<svg ...>...</svg>`) }}
    />
    ```
  </PreviewCode>
  <PreviewCode slot="tab" label="HTML">
    ```html
    <img
      class="set--mask -ar:og"
      src="https://cdn.lism-css.com/img/u-2.jpg"
      width="960"
      height="640"
      loading="lazy"
      style="
        --maskImg: url(data:image/svg+xml;base64,);
      "
    />
    ```
  </PreviewCode>
</Preview>
 */}


