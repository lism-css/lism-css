---
title: 'Container'
description: 'Lism CSS の Container モジュールについて解説します。container-type を設定しコンテナクエリを有効にするコンポーネントです。'
---
import { Container, Wrapper, Box, Lism, Spacer } from "lism-css/astro";

`container-type`をセットできる[`is--container`](/docs/module-class/#is--container) クラスをコンポーネントから利用する例を紹介していきます。

`is--container` は基本的に `isContainer`プロパティで**どのコンポーネントからでも呼び出せるようになっています。**

| プロパティ | 出力 |
|---|---|
| `isContainer`| `.is--container` |

さらに、`<Lism isContainer>` のエイリアスとして、`<Container>` も用意しています。

ここでは、この専用コンポーネントを紹介していきます。



## Import

<ImportPackage component='Container' />


## Usage

`isContainer`をLismコンポーネントにセットするか、または`<Container>`コンポーネントを使用して`is--container`が付与され要素の内部では、コンテナクエリが有効になります。

<Preview>
  <PreviewTitle>例</PreviewTitle>
  <PreviewArea resize p='15'>
    <Container isWrapper="s" p="20">
      <Box bd p={['10', '30']}>
        <Fragment>このBOXは、paddingが切り替わります。</Fragment>
      </Box>
    </Container>
  </PreviewArea>
  <PreviewCode slot='tab' label='JSX'>
    ```jsx
    <Container isWrapper="s" p="20">
      <Box bd p={['10', '30']}>
        このBOXは、paddingが切り替わります。
      </Box>
    </Container>
    ```
  </PreviewCode>
  <PreviewCode slot='tab' label='HTML'>
    ```html
    <div class="is--container is--wrapper -contentSize:s -p:20">
      <div class="l--box -bd -p:10 -p_sm" style="--p_sm: var(--s30)">このBOXは、paddingが切り替わります。</div>
    </div>
    ```
  </PreviewCode>
</Preview>
