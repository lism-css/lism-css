---
title: 'Inline'
description: '<span>タグを出力するLismコンポーネントです。'
order: 3
---
import { Text, Inline, Lism, Dummy } from "lism-css/astro";

`<Inline>`は、`<span>`タグを出力するLismコンポーネントです。  
テキストの一部にスタイルを適用したい場合などに使用します。

:::info
`<Inline>` は `<Lism as='span'>` のエイリアスです。全ての [Lism Props](/docs/core-components/Lism/) が使用できます。  
`as` prop で出力タグを変更できます: `span`(デフォルト) / `em` / `strong` / `small` / `code` / `time` / `i` / `b` / `mark` / `abbr` / `cite` / `kbd`
:::

## Import

<ImportPackage component='Inline' />


## 基本的な使い方

<Preview>
  <PreviewTitle>`<Inline>`の出力例</PreviewTitle>
  <PreviewArea p='20'>
    <Text>これは<Inline fw="bold" c="blue">強調された</Inline>テキストです。</Text>
  </PreviewArea>
  <PreviewCode slot='tab' label='JSX'>
    ```jsx
    <Text>これは<Inline fw="bold" c="blue">強調された</Inline>テキストです。</Text>
    ```
  </PreviewCode>
  <PreviewCode slot='tab' label='HTML'>
    ```html
    <p>これは<span class="-fw:bold -c" style="--c:var(--blue)">強調された</span>テキストです。</p>
    ```
  </PreviewCode>
</Preview>

<Preview>
  <PreviewTitle>Lism Propsの適用例</PreviewTitle>
  <PreviewArea p='20'>
    <Text>
      <>テキストの中で</>
      <Inline fz="l" bgc="pale" p="5" bdrs="5">インライン要素</Inline>
      <>としてスタイルできます。</>
    </Text>
  </PreviewArea>
  <PreviewCode slot='tab' label='JSX'>
    ```jsx
    <Text>
      テキストの中で
      <Inline fz="l" bgc="pale" p="5" bdrs="5">インライン要素</Inline>
      としてスタイルできます。
    </Text>
    ```
  </PreviewCode>
</Preview>
