---
title: 'Wrapper (is--wrapper)'
navtitle: 'is--wrapper'
description: 'is--wrapper は、その直下のコンテンツ幅を一括制御できるクラスです。'
---
import { Wrapper, Box, Lism, Spacer } from "lism-css/astro";

`is--wrapper` は、その直下のコンテンツ幅を一括制御できるクラスです。


## CSS

<SrcCode path="src/scss/trait/is/_wrapper.scss" />

直下の子要素には `inline-size: 100%` が当たるため、自然幅が `--contentSize` 未満の要素（短い段落や `<table>` など）も常に親幅まで広がります。これにより `l--stack` などの縦並び配下でも横幅が揃い、ガタつきが起きません。

:::caution
`<table>` を `is--wrapper` の直下に置くと内容依存の自然幅にはならず、常に wrapper 幅まで広がります。テーブルの自然幅を保ちたい場合は、`is--wrapper` を直接の親にしないように間に別の要素を挟んでください。
:::


## Lismコンポーネントでの使い方

`isWrapper` プロパティで**どのコンポーネントからでも呼び出せるようになっています。**

| プロパティ | 出力 |
|---|---|
| `isWrapper`| `is--wrapper` |
| `isWrapper='s'` | `is--wrapper`, `-contentSize:s` |
| `isWrapper='l'` | `is--wrapper`, `-contentSize:l` |
| `isWrapper='xl'` | `is--wrapper`, `-contentSize:xl` |
| `isWrapper={value}` | `is--wrapper`, `--contentSize:{value}` |

`<Lism isWrapper>` のエイリアスとして、`<Wrapper>` も用意しています。

### Import

<ImportPackage component="Wrapper" />

### 専用Props

| プロパティ | 説明 |
|---|---|
| `contentSize` | コンテンツサイズを指定します。|
| `layout` | 展開したいレイアウトプリミティブを指定できます。|
| `isContainer` | コンテナタイプをセットできます。|

<PreviewTitle>下記の ①, ② はそれぞれ同じ結果になります。</PreviewTitle>

```jsx
// ①
<Flow isWrapper="s" isContainer>...</Flow>

// ②
<Wrapper contentSize="s" layout="flow" isContainer>...</Wrapper>
```


## Usage

### 使用例

<Preview>
  <PreviewTitle>例</PreviewTitle>
  <PreviewArea resize p="15">
    <Wrapper layout="flow" p="20" className="-bd:guide -hov:itemGuide">
      <p>Content</p>
      <p>Content</p>
      <p>...</p>
    </Wrapper>
  </PreviewArea>
  <PreviewCode slot="tab" label="JSX">
    ```jsx
    <Wrapper layout="flow" p="20">
      <p>Content</p>
      <p>Content</p>
      <p>...</p>
    </Wrapper>
    ```
  </PreviewCode>
  <PreviewCode slot="tab" label="HTML">
    ```html
    <div class="l--flow is--wrapper -p:20">
      <p>Content</p>
      <p>Content</p>
      <p>...</p>
    </div>
    ```
  </PreviewCode>
</Preview>

<Preview>
  <PreviewTitle>`contentSize='s'`を指定</PreviewTitle>
  <PreviewArea resize p="15">
    <Wrapper contentSize="s" layout="flow" p="20" className="-bd:guide -hov:itemGuide">
      <p>Content</p>
      <p>Content</p>
      <p>...</p>
    </Wrapper>
  </PreviewArea>
  <PreviewCode slot="tab" label="JSX">
    ```jsx
    <Wrapper contentSize="s" layout="flow" p="20">
      <p>Content</p>
      <p>Content</p>
      <p>...</p>
    </Wrapper>
    ```
  </PreviewCode>
  <PreviewCode slot="tab" label="HTML">
    ```html
    <div class="l--flow is--wrapper -contentSize:s -p:20">
      <p>Content</p>
      <p>Content</p>
      <p>...</p>
    </div>
    ```
  </PreviewCode>
</Preview>

### 任意のコンテナサイズをセットする

s, l, xl 以外の値をセットしたい場合は、isWrapper に任意の値をセットすると`--contentSize`変数として出力されます。

<Preview>
  <PreviewTitle>is--wrapper に任意の値をセットする</PreviewTitle>
  <PreviewArea resize p="15">
    <Lism isWrapper="20rem" p="20" className="-bd:guide -hov:itemGuide">
      <div>Contents...</div>
      <div>Contents...</div>
    </Lism>
  </PreviewArea>
  <PreviewCode slot="tab" label="JSX">
  ```jsx
  <Lism isWrapper="20rem" p="20">
    <div>Contents...</div>
    <div>Contents...</div>
  </Lism>
  ```
  </PreviewCode>
  <PreviewCode slot="tab" label="HTML">
  ```html
  <div class="is--wrapper -p:20" style="--contentSize: 20rem">
    <div>Contents...</div>
    <div>Contents...</div>
  </div>
  ```
  </PreviewCode>
</Preview>


### DEMOページ

:::info
`is--wrapper`関連の挙動を確認できるデモページを用意しています。  
ぜひ広い画面でご確認ください。
:::

<PreviewFrame url="/demo/container" />
