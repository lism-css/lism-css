---
/**
 * デモページ用レイアウト
 * - テンプレートプレビューやデモページで使用
 * - シンプルなLism CSSのみを読み込み、noindexを設定
 */
import 'lism-css/main.css';
// index 経由だと他の Preview コンポーネントの CSS が先に入り @layer の宣言順が崩れるため、ファイルを直接読み込む
import PreviewSizeReporter from '@/components/Preview/PreviewSizeReporter.astro';

const { title = '', lang = 'ja' } = Astro.props;
---

<html lang={lang}>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="robots" content="noindex,nofollow" />
    <title>Lism CSS - {title}</title>
    <style is:global>
      .-bg\:stripe {
        background-image: repeating-linear-gradient(-55deg, var(--divider), var(--divider) 1px, transparent 1px, transparent 6px) !important;
      }
    </style>
  </head>
  <body>
    <PreviewSizeReporter>
      <slot />
    </PreviewSizeReporter>
  </body>
</html>
