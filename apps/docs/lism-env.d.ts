// このファイルは lism-css が lism.config.js の breakpoints / props / traits / tokens から自動生成します。
// 編集しないでください（次回の dev / build 時に上書きされます）。
import 'lism-css';

declare module 'lism-css' {
  interface CustomPropValueRegistry {
    lts: '2xl';
    p: 'box';
  }
}
