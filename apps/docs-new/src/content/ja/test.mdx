---
title: "test"
description: "..."
date: 2025-12-01
tags: []
draft: true
---

:::note
これはメモです。
:::

:::point
これはメモです。
:::

:::alert
これはメモです。
:::

:::warning
これはメモです。
:::

:::check
これはメモです。
:::

:::help
::title[タイトルだよ `hoge`]

これはメモです。
:::

スクロールバーの幅問題については、JSを使って対処するか、もしくはモダンCSSであれば`@property`と`cqw`(`cqi`)を使った変数定義でbodyサイズを記憶させておくというアプローチもあります。(後ほど軽く解説します)

:::note
スクロールバーの幅問題については、JSを使って対処するか、もしくはモダンCSSであれば`@property`と`cqw`(`cqi`)を使った変数定義でbodyサイズを記憶させておくというアプローチもあります。(後ほど軽く解説します)
:::


---


<CanUse prop=":has()" chrome="105" safari="15.4" firefox="121" />

---


```html
<html lang={siteConfig.lang}>
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="viewport" content="width=device-width" />
	</head>
	<body>
		<slot />
	</body>
</html>
```

```css title="CSS"
:root {
	/* 基準となるフォントサイズをclamp() で定義 */
	--fz--base: clamp(1rem, 0.654rem + 1.54vw, 2rem); /* わかりやすく大袈裟に変化させています */

	/* --fz-base を元にスケーリングさせる */
	--fz--xs: calc(var(--fz--base) * (8 / 10));
	--fz--s: calc(var(--fz--base) * (8 / 9));
	--fz--l: calc(var(--fz--base) * (8 / 7));
	--fz--xl: calc(var(--fz--base) * (8 / 6));
}

/* ユーティリティクラス */
.fz--base {
	font-size: var(--fz--base);
}
.fz--xs {
	font-size: var(--fz--xs);
}
.fz--s {
	font-size: var(--fz--s);
}

```


## 直接プレビュー

<Preview useLism title='padding, gap にどちらも同じ余白を設定しているが、左右と上下で余白感が異なる例' css={`
.l--stack > *{line-height: 1.8}
`}>
<div class="l--stack -bd -p:30 -g:30 -bdrs:10">
	<h2 class="-fz:l">タイトル文字列</h2>
	<p>ロレム・イプサムの座り雨。目まぐるしい文章の流れの中で、それは静かに歩く仮の言葉です。Elitも穏やかに続いていきますが、積み重ねられてきた「LiberroyとFoogの取り組み」は、余白のようなものです。</p>
	<p class="-fz:s">フッターテキスト</p>
</div>

<PreviewCode slot="code" label="HTML">
```html
<div class="l--stack -bd -p:30 -g:30 -bdrs:10">
	<h2 class="-fz:l">タイトル文字列</h2>
	<p>ロレム・イプサムの座り雨。目まぐるしい文章の流れの中で、それは静かに歩く仮の言葉です。Elitも穏やかに続いていきますが、積み重ねられてきた「LiberroyとFoogの取り組み」は、余白のようなものです。</p>
	<p class="-fz:s">フッターテキスト</p>
</div>
```
</PreviewCode>
</Preview>


## iframe プレビュー

<Preview src="clamp/ex-tokens" resize ar='2/1' _addFiles={['./../hogehogheogheoghe.html','./../foooooo.html']}/>
