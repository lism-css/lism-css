---
title: 'Tabs'
description: 'A UI component that switches content panels with tabs.'
---

import { Icon } from 'lism-css/astro';
import { Tabs } from '@lism-css/ui/astro/Tabs';
import { DummyText } from '@lism-css/ui/astro/DummyText';
import '../../ja/ui/_opt-in/_tabs-emboss.css'
import '../../ja/ui/_opt-in/_tabs-folder.css'
import '../../ja/ui/_opt-in/_tabs-keep-height.css'


A component for creating tab elements.  
Only interaction behavior is provided — visual styling is intentionally left for you to customize.


## Overview

<Preview>
  <PreviewTitle>Preview</PreviewTitle>
  <PreviewArea p="30">
    <Tabs.Root>
      <Tabs.Item>
        <Tabs.Tab>Tab 1</Tabs.Tab>
        <Tabs.Panel>
          <DummyText pre="Tab 01: " />
        </Tabs.Panel>
      </Tabs.Item>
      <Tabs.Item>
        <Tabs.Tab>Tab 2</Tabs.Tab>
        <Tabs.Panel>
          <DummyText length="l" offset={1} pre="Tab 02: " />
        </Tabs.Panel>
      </Tabs.Item>
      <Tabs.Item>
        <Tabs.Tab>Tab 3</Tabs.Tab>
        <Tabs.Panel>
          <DummyText length="l" offset={2} pre="Tab 03: " />
        </Tabs.Panel>
      </Tabs.Item>
    </Tabs.Root>
  </PreviewArea>
  <PreviewCode slot="tab" label="JSX">
    ```jsx
    <Tabs.Root>
      <Tabs.Item>
        <Tabs.Tab>Tab 1</Tabs.Tab>
        <Tabs.Panel>
          <DummyText pre="Tab 01: "/>
        </Tabs.Panel>
      </Tabs.Item>
      <Tabs.Item>
        <Tabs.Tab>Tab 2</Tabs.Tab>
        <Tabs.Panel>
          <DummyText length="l" offset={1} pre="Tab 02: " />
        </Tabs.Panel>
      </Tabs.Item>
      <Tabs.Item>
        <Tabs.Tab>Tab 3</Tabs.Tab>
        <Tabs.Panel>
          <DummyText length="l" offset={2} pre="Tab 03: " />
        </Tabs.Panel>
      </Tabs.Item>
    </Tabs.Root>
    ```
  </PreviewCode>
  <PreviewCode slot="tab" label="HTML">
```html
<div class="b--tabs b--tabs--default l--grid">
  <div class="b--tabs_list" role="tablist">
    <button class="b--tabs_tab set--plain" type="button" id="{id}-1-tab" role="tab" aria-controls="{id}-1" aria-selected="true" tabindex="0">Tab 1</button>
    <button class="b--tabs_tab set--plain" type="button" id="{id}-2-tab" role="tab" aria-controls="{id}-2" aria-selected="false" tabindex="-1">Tab 2</button>
    <button class="b--tabs_tab set--plain" type="button" id="{id}-3-tab" role="tab" aria-controls="{id}-3" aria-selected="false" tabindex="-1">Tab 3</button>
  </div>
  <div class="b--tabs_panel" id="{id}-1" role="tabpanel" aria-labelledby="{id}-1-tab" tabindex="0">...Contents...</div>
  <div class="b--tabs_panel" id="{id}-2" role="tabpanel" aria-labelledby="{id}-2-tab" tabindex="0" hidden>...Contents...</div>
  <div class="b--tabs_panel" id="{id}-3" role="tabpanel" aria-labelledby="{id}-3-tab" tabindex="0" hidden>...Contents...</div>
</div>
```
  </PreviewCode>
</Preview>

:::warning
Only `<Tabs.Tab>` and `<Tabs.Panel>` are recognized as children of `<Tabs.Item>`. Both must always be used together.
:::


## Styles

The Tabs base styles are defined in the following CSS.

<SrcCode dir="lism-ui" path="src/components/Tabs/_style.css" title="_style.css" />

:::note
The full source code is available on [GitHub](https://github.com/lism-css/core/tree/main/packages/lism-ui/src/components/Tabs).
:::


## Usage

Provided as `Tabs` from the `@lism-css/ui` package.


### Import

<ImportPackage component="Tabs" script="tabs.js" packageName="@lism-css/ui" css="style.css" />


### Props

| Prop | Description |
|---|---|
|<PropBadge>`<Tabs.Root>`</PropBadge><br/>`variant`| Outputs the `b--tabs--{variant}` class. Defaults to `default`, and `line` is also provided out of the box. When you specify your own variant, the default variant's decoration (`b--tabs--default`) no longer applies, so you can build your own styles without resetting the initial ones. |
|<PropBadge>`<Tabs.Root>`</PropBadge><br/>`tabId`| A string ID used to generate each tab button's id (`{tabId}-{index}-tab`), each panel's id (`{tabId}-{index}`), and the value of `aria-controls`. If omitted, an ID is generated automatically. |
|<PropBadge>`<Tabs.Root>`</PropBadge><br/>`defaultIndex`| Specifies which tab to open initially. Values out of range (e.g. a number greater than the number of tabs) fall back to `1`. |
|<PropBadge>`<Tabs.Root>`</PropBadge><br/>`listProps`| Props passed to the list element (`b--tabs_list`) wrapping the tab buttons. |
|<PropBadge>`<Tabs.Tab>`</PropBadge> <PropBadge>`<Tabs.Panel>`</PropBadge><br/>`index`| A tab number starting from `1`. Specify the same number on the corresponding `<Tabs.Tab>` and `<Tabs.Panel>`. In a normal `<Tabs.Item>` composition this is set automatically, so you don't need to specify it. It's required when [composing without `<Tabs.Item>`](#compose-without-tabsitem). |
|<PropBadge>`<Tabs.Tab>`</PropBadge> <PropBadge>`<Tabs.Panel>`</PropBadge><br/>`tabId`| The ID used to generate the id (`{tabId}-{index}-tab` / `{tabId}-{index}`). In a normal `<Tabs.Item>` composition, the value from `<Tabs.Root>` is set automatically, so you don't need to specify it. When composing without `<Tabs.Item>`, this is only required in the Astro version. |
|<PropBadge>`<Tabs.Tab>`</PropBadge> <PropBadge>`<Tabs.Panel>`</PropBadge><br/>`isActive`| Specifies whether this tab should be open initially. In a normal `<Tabs.Item>` composition, this is set automatically based on `defaultIndex`, so you don't need to specify it. When composing without `<Tabs.Item>` in the Astro version, set this on the pair of `<Tabs.Tab>` and `<Tabs.Panel>` that should be open initially. |
{/* |<PropBadge>`<Tabs.Item>`</PropBadge><br/>`label` | Specifies the tab label text. | */}

{/* |<PropBadge>`<Tabs.Root>`</PropBadge><br/>`keepHeight`| Outputs `[data-tabs-height="keep"]`, fixing the overall height of the tabs to the tallest content panel. | */}


### Keyboard interactions

When a tab button has focus, the following keyboard interactions are available.

| Key | Action |
|---|---|
|<kbd>←</kbd> <kbd>→</kbd>| Moves focus to the previous or next tab and selects it, looping to the opposite end when it reaches the first or last tab. |
|<kbd>Home</kbd>| Moves to and selects the first tab. |
|<kbd>End</kbd>| Moves to and selects the last tab. |

For a vertical tab list, pass `aria-orientation="vertical"` through `listProps`. The arrow keys then switch to <kbd>↑</kbd> and <kbd>↓</kbd>.

```jsx
<Tabs.Root listProps={{ 'aria-orientation': 'vertical' }}>
```

:::note
A horizontal tab list (the default) deliberately ignores <kbd>↑</kbd> and <kbd>↓</kbd> so that those keys keep their normal browser scrolling behavior.

Note that `aria-orientation` is an HTML attribute, so its value cannot change with the viewport width. For layouts that switch between horizontal and vertical responsively, keep the default (horizontal, <kbd>←</kbd> <kbd>→</kbd>).
:::


## Examples

### variant="line"

Specify `variant="line"` for a line-style tab list that underlines the selected tab.

<Preview>
  <PreviewTitle>`variant='line'`</PreviewTitle>
  <PreviewArea p="30">
    <Tabs.Root variant="line">
      <Tabs.Item>
        <Tabs.Tab>Tab 1</Tabs.Tab>
        <Tabs.Panel>
          <DummyText pre="Tab 01: " />
        </Tabs.Panel>
      </Tabs.Item>
      <Tabs.Item>
        <Tabs.Tab>Tab 2</Tabs.Tab>
        <Tabs.Panel>
          <DummyText length="l" offset={1} pre="Tab 02: " />
        </Tabs.Panel>
      </Tabs.Item>
      <Tabs.Item>
        <Tabs.Tab>Tab 3</Tabs.Tab>
        <Tabs.Panel>
          <DummyText length="l" offset={2} pre="Tab 03: " />
        </Tabs.Panel>
      </Tabs.Item>
    </Tabs.Root>
  </PreviewArea>
  <PreviewCode slot="tab" label="JSX">
    ```jsx 'variant="line"'
    <Tabs.Root variant="line">
      <Tabs.Item>
        <Tabs.Tab>Tab 1</Tabs.Tab>
        <Tabs.Panel>
          <DummyText pre="Tab 01: " />
        </Tabs.Panel>
      </Tabs.Item>
      <Tabs.Item>
        <Tabs.Tab>Tab 2</Tabs.Tab>
        <Tabs.Panel>
          <DummyText length="l" offset={1} pre="Tab 02: " />
        </Tabs.Panel>
      </Tabs.Item>
      <Tabs.Item>
        <Tabs.Tab>Tab 3</Tabs.Tab>
        <Tabs.Panel>
          <DummyText length="l" offset={2} pre="Tab 03: " />
        </Tabs.Panel>
      </Tabs.Item>
    </Tabs.Root>
    ```
  </PreviewCode>
  <PreviewCode slot="tab" label="HTML">
```html
<div class="b--tabs b--tabs--line l--grid">
  <div class="b--tabs_list" role="tablist">
    <button class="b--tabs_tab set--plain" type="button" id="{id}-1-tab" role="tab" aria-controls="{id}-1" aria-selected="true" tabindex="0">Tab 1</button>
    <button class="b--tabs_tab set--plain" type="button" id="{id}-2-tab" role="tab" aria-controls="{id}-2" aria-selected="false" tabindex="-1">Tab 2</button>
    <button class="b--tabs_tab set--plain" type="button" id="{id}-3-tab" role="tab" aria-controls="{id}-3" aria-selected="false" tabindex="-1">Tab 3</button>
  </div>
  <div class="b--tabs_panel" id="{id}-1" role="tabpanel" aria-labelledby="{id}-1-tab" tabindex="0">...Contents...</div>
  <div class="b--tabs_panel" id="{id}-2" role="tabpanel" aria-labelledby="{id}-2-tab" tabindex="0" hidden>...Contents...</div>
  <div class="b--tabs_panel" id="{id}-3" role="tabpanel" aria-labelledby="{id}-3-tab" tabindex="0" hidden>...Contents...</div>
</div>
```
  </PreviewCode>
</Preview>


### Default open tab

Use `defaultIndex` on `<Tabs.Root>` to specify which tab is open by default.  
(Index numbers start from `1`.)

<Preview>
  <PreviewTitle>Example</PreviewTitle>
  <PreviewArea p="30">
    <Tabs.Root defaultIndex={2}>
      <Tabs.Item>
        <Tabs.Tab>Tab 1</Tabs.Tab>
        <Tabs.Panel>
          <DummyText pre="Tab 01: " />
        </Tabs.Panel>
      </Tabs.Item>
      <Tabs.Item>
        <Tabs.Tab>Tab 2</Tabs.Tab>
        <Tabs.Panel>
          <DummyText length="l" offset={1} pre="Tab 02: " />
        </Tabs.Panel>
      </Tabs.Item>
      <Tabs.Item>
        <Tabs.Tab>Tab 3</Tabs.Tab>
        <Tabs.Panel>
          <DummyText length="l" offset={2} pre="Tab 03: " />
        </Tabs.Panel>
      </Tabs.Item>
    </Tabs.Root>
  </PreviewArea>
  <PreviewCode slot="tab" label="JSX">
    ```jsx "defaultIndex=\{2\}"
    <Tabs.Root defaultIndex={2}>
      <Tabs.Item>
        <Tabs.Tab>Tab 1</Tabs.Tab>
        <Tabs.Panel>
          <DummyText pre="Tab 01: "/>
        </Tabs.Panel>
      </Tabs.Item>
      <Tabs.Item>
        <Tabs.Tab>Tab 2</Tabs.Tab>
        <Tabs.Panel>
          <DummyText length="l" offset={1} pre="Tab 02: " />
        </Tabs.Panel>
      </Tabs.Item>
      <Tabs.Item>
        <Tabs.Tab>Tab 3</Tabs.Tab>
        <Tabs.Panel>
          <DummyText length="l" offset={2} pre="Tab 03: " />
        </Tabs.Panel>
      </Tabs.Item>
    </Tabs.Root>
    ```
  </PreviewCode>
  <PreviewCode slot="tab" label="HTML">
```html
<div class="b--tabs b--tabs--default l--grid">
  <div class="b--tabs_list" role="tablist">
    <button class="b--tabs_tab set--plain" type="button" id="{id}-1-tab" role="tab" aria-controls="{id}-1" aria-selected="false" tabindex="-1">Tab 1</button>
    <button class="b--tabs_tab set--plain" type="button" id="{id}-2-tab" role="tab" aria-controls="{id}-2" aria-selected="true" tabindex="0">Tab 2</button>
    <button class="b--tabs_tab set--plain" type="button" id="{id}-3-tab" role="tab" aria-controls="{id}-3" aria-selected="false" tabindex="-1">Tab 3</button>
  </div>
  <div class="b--tabs_panel" id="{id}-1" role="tabpanel" aria-labelledby="{id}-1-tab" tabindex="0" hidden>...Contents...</div>
  <div class="b--tabs_panel" id="{id}-2" role="tabpanel" aria-labelledby="{id}-2-tab" tabindex="0">...Contents...</div>
  <div class="b--tabs_panel" id="{id}-3" role="tabpanel" aria-labelledby="{id}-3-tab" tabindex="0" hidden>...Contents...</div>
</div>
```
  </PreviewCode>
</Preview>


### Specify the initial tab via URL parameter

Opening a page with `?lism-tab={tabId}-{index}` in the URL displays the specified tab as selected from the start.

To use this feature, explicitly set `tabId` on `<Tabs.Root>`.  
(If `tabId` is omitted, an ID is generated automatically, so it can't be targeted from the URL.)

```jsx 'tabId="sample-tabs"'
<Tabs.Root tabId="sample-tabs">
  <Tabs.Item>
    <Tabs.Tab>Tab 1</Tabs.Tab>
    <Tabs.Panel>...</Tabs.Panel>
  </Tabs.Item>
  <Tabs.Item>
    <Tabs.Tab>Tab 2</Tabs.Tab>
    <Tabs.Panel>...</Tabs.Panel>
  </Tabs.Item>
  <Tabs.Item>
    <Tabs.Tab>Tab 3</Tabs.Tab>
    <Tabs.Panel>...</Tabs.Panel>
  </Tabs.Item>
</Tabs.Root>
```

Visiting a page with the tabs above using `?lism-tab=sample-tabs-2` displays the second tab as selected.


### Compose without Tabs.Item

You can also place `<Tabs.List>` (a list with `role="tablist"`), `<Tabs.Tab>`, and `<Tabs.Panel>` directly inside `<Tabs.Root>` instead of using `<Tabs.Item>`.  
Use this when you want to control the output structure yourself — for example, to wrap tab buttons in `<li>` elements, or to insert another element between the list and the panels.

In this composition, matching `<Tabs.Tab>` and `<Tabs.Panel>` pairs are linked by `index` (a number starting from `1`). Tab switching on click or keyboard interaction, focus movement, and ARIA attribute updates are all handled by the component, so you don't need to write any state management or event handling yourself.

<Preview>
  <PreviewTitle>Example</PreviewTitle>
  <PreviewArea p="30">
    <Tabs.Root>
      <Tabs.List>
        <Tabs.Tab tabId="manual-tabs" index={1}>Tab 1</Tabs.Tab>
        <Tabs.Tab tabId="manual-tabs" index={2} isActive>Tab 2</Tabs.Tab>
        <Tabs.Tab tabId="manual-tabs" index={3}>Tab 3</Tabs.Tab>
      </Tabs.List>
      <Tabs.Panel tabId="manual-tabs" index={1}>
        <DummyText pre="Tab 01: " />
      </Tabs.Panel>
      <Tabs.Panel tabId="manual-tabs" index={2} isActive>
        <DummyText length="l" offset={1} pre="Tab 02: " />
      </Tabs.Panel>
      <Tabs.Panel tabId="manual-tabs" index={3}>
        <DummyText length="l" offset={2} pre="Tab 03: " />
      </Tabs.Panel>
    </Tabs.Root>
  </PreviewArea>
  <PreviewCode slot="tab" label="React">
    ```jsx
    <Tabs.Root tabId="manual-tabs" defaultIndex={2}>
      <Tabs.List>
        <Tabs.Tab index={1}>Tab 1</Tabs.Tab>
        <Tabs.Tab index={2}>Tab 2</Tabs.Tab>
        <Tabs.Tab index={3}>Tab 3</Tabs.Tab>
      </Tabs.List>
      <Tabs.Panel index={1}>
        <DummyText pre="Tab 01: " />
      </Tabs.Panel>
      <Tabs.Panel index={2}>
        <DummyText length="l" offset={1} pre="Tab 02: " />
      </Tabs.Panel>
      <Tabs.Panel index={3}>
        <DummyText length="l" offset={2} pre="Tab 03: " />
      </Tabs.Panel>
    </Tabs.Root>
    ```
  </PreviewCode>
  <PreviewCode slot="tab" label="Astro">
    ```astro
    <Tabs.Root>
      <Tabs.List>
        <Tabs.Tab tabId="manual-tabs" index={1}>Tab 1</Tabs.Tab>
        <Tabs.Tab tabId="manual-tabs" index={2} isActive>Tab 2</Tabs.Tab>
        <Tabs.Tab tabId="manual-tabs" index={3}>Tab 3</Tabs.Tab>
      </Tabs.List>
      <Tabs.Panel tabId="manual-tabs" index={1}>
        <DummyText pre="Tab 01: " />
      </Tabs.Panel>
      <Tabs.Panel tabId="manual-tabs" index={2} isActive>
        <DummyText length="l" offset={1} pre="Tab 02: " />
      </Tabs.Panel>
      <Tabs.Panel tabId="manual-tabs" index={3}>
        <DummyText length="l" offset={2} pre="Tab 03: " />
      </Tabs.Panel>
    </Tabs.Root>
    ```
  </PreviewCode>
  <PreviewCode slot="tab" label="HTML">
```html
<div class="b--tabs b--tabs--default l--grid">
  <div class="b--tabs_list" role="tablist">
    <button class="b--tabs_tab set--plain" type="button" id="manual-tabs-1-tab" role="tab" aria-controls="manual-tabs-1" aria-selected="false" tabindex="-1">Tab 1</button>
    <button class="b--tabs_tab set--plain" type="button" id="manual-tabs-2-tab" role="tab" aria-controls="manual-tabs-2" aria-selected="true" tabindex="0">Tab 2</button>
    <button class="b--tabs_tab set--plain" type="button" id="manual-tabs-3-tab" role="tab" aria-controls="manual-tabs-3" aria-selected="false" tabindex="-1">Tab 3</button>
  </div>
  <div class="b--tabs_panel" id="manual-tabs-1" role="tabpanel" aria-labelledby="manual-tabs-1-tab" tabindex="0" hidden>...Contents...</div>
  <div class="b--tabs_panel" id="manual-tabs-2" role="tabpanel" aria-labelledby="manual-tabs-2-tab" tabindex="0">...Contents...</div>
  <div class="b--tabs_panel" id="manual-tabs-3" role="tabpanel" aria-labelledby="manual-tabs-3-tab" tabindex="0" hidden>...Contents...</div>
</div>
```
  </PreviewCode>
</Preview>

The required props differ between the React and Astro versions.

| | React | Astro |
|---|---|---|
|`index`| Required on every `<Tabs.Tab>` and `<Tabs.Panel>`. | Required on every `<Tabs.Tab>` and `<Tabs.Panel>`. |
|`tabId`| Not needed — the value from `<Tabs.Root>` (or its auto-generated ID, if omitted) is shared automatically. Even if you set it individually, `<Tabs.Root>` takes priority. | Specify the same value on each `<Tabs.Tab>` and `<Tabs.Panel>`. |
|Initial selection| Set via `defaultIndex` (default `1`) on `<Tabs.Root>`. | Set `isActive` on both the `<Tabs.Tab>` and `<Tabs.Panel>` that should be open initially. |

- In the React version, `<Tabs.Tab>` with `isActive` becomes the initial selection only when `defaultIndex` is omitted. This compatibility feature lets you reuse the same markup as the Astro version, but using `defaultIndex` is recommended for React. `isActive` on `<Tabs.Panel>` isn't needed in the React version.
- `onClick` and `onKeyDown` handlers passed to `<Tabs.Tab>` in the React version are called before the component's internal handling. Calling `e.preventDefault()` inside them prevents the tab switch (the same applies to the `<Tabs.Item>` composition).
- The Astro version can't share values from parent to child, so `tabId`, `defaultIndex`, and `listProps` on `<Tabs.Root>` have no effect in this composition. Pass any props you want on the list element directly to `<Tabs.List>` instead.
- [Specifying the initial tab via a URL parameter](#specify-the-initial-tab-via-url-parameter) also works with this composition. When using it in the React version, explicitly set `tabId` on `<Tabs.Root>`.

:::warning
- Always place `<Tabs.Tab>` inside `<Tabs.List>`. Keyboard navigation follows the tabs within the same `role="tablist"` element in DOM order.
- This composition doesn't fall back to `1` for out-of-range numbers. If you set the initial selection to a number that doesn't exist, the tabs render with none selected (you can still select one by clicking).
:::


### Vertical tabs

Setting `grid-template:'list panel' auto / auto 1fr;` on `<Tabs.Root>` (`b--tabs`) places the tab list and content side by side.

At the same time, changing the tab list (`b--tabs_list`) buttons to stack vertically (`flex-direction:column;`) achieves the following layout.

<Preview>
  <PreviewTitle>Example: side-by-side layout on `@sm` and above</PreviewTitle>
  <PreviewArea p="30" resize>
    <Tabs.Root gt={[null, '"list panel" auto / auto 1fr']} listProps={{ fxd: [null, 'column'] }}>
      <Tabs.Item>
        <Tabs.Tab>
          <Fragment>Tab 1</Fragment>
        </Tabs.Tab>
        <Tabs.Panel>
          <DummyText pre="Tab 01: " />
        </Tabs.Panel>
      </Tabs.Item>
      <Tabs.Item>
        <Tabs.Tab>
          <Fragment>Tab 2</Fragment>
        </Tabs.Tab>
        <Tabs.Panel>
          <DummyText length="l" offset={1} pre="Tab 02: " />
        </Tabs.Panel>
      </Tabs.Item>
      <Tabs.Item>
        <Tabs.Tab>
          <Fragment>Tab 3</Fragment>
        </Tabs.Tab>
        <Tabs.Panel>
          <DummyText length="l" offset={2} pre="Tab 03: " />
        </Tabs.Panel>
      </Tabs.Item>
    </Tabs.Root>
  </PreviewArea>
  <PreviewCode slot="tab" label="JSX">
    ```jsx
    <Tabs.Root gt={[null, '"list panel" auto / auto 1fr']} listProps={{ fxd: [null, 'column'] }}>
      <Tabs.Item>
        <Tabs.Tab>Tab 1</Tabs.Tab>
        <Tabs.Panel>
          <DummyText pre="Tab 01: "/>
        </Tabs.Panel>
      </Tabs.Item>
      <Tabs.Item>
        <Tabs.Tab>Tab 2</Tabs.Tab>
        <Tabs.Panel>
          <DummyText length="l" offset={1} pre="Tab 02: " />
        </Tabs.Panel>
      </Tabs.Item>
      <Tabs.Item>
        <Tabs.Tab>Tab 3</Tabs.Tab>
        <Tabs.Panel>
          <DummyText length="l" offset={2} pre="Tab 03: " />
        </Tabs.Panel>
      </Tabs.Item>
    </Tabs.Root>
    ```
  </PreviewCode>
  <PreviewCode slot="tab" label="HTML">
```html
<div class="b--tabs b--tabs--default l--grid -gt_sm" style='--gt_sm:"list panel" auto / auto 1fr'>
  <div class="b--tabs_list -fxd_sm" style="--fxd_sm:column" role="tablist">
    <button class="b--tabs_tab set--plain" type="button" id="{id}-1-tab" role="tab" aria-controls="{id}-1" aria-selected="true" tabindex="0">Tab 1</button>
    <button class="b--tabs_tab set--plain" type="button" id="{id}-2-tab" role="tab" aria-controls="{id}-2" aria-selected="false" tabindex="-1">Tab 2</button>
    <button class="b--tabs_tab set--plain" type="button" id="{id}-3-tab" role="tab" aria-controls="{id}-3" aria-selected="false" tabindex="-1">Tab 3</button>
  </div>
  <div class="b--tabs_panel" id="{id}-1" role="tabpanel" aria-labelledby="{id}-1-tab" tabindex="0">...Contents...</div>
  <div class="b--tabs_panel" id="{id}-2" role="tabpanel" aria-labelledby="{id}-2-tab" tabindex="0" hidden>...Contents...</div>
  <div class="b--tabs_panel" id="{id}-3" role="tabpanel" aria-labelledby="{id}-3-tab" tabindex="0" hidden>...Contents...</div>
</div>
```
  </PreviewCode>
</Preview>


## Opt-in

The following are example style variations you can build yourself.

`b--tabs_tab` exposes `--_isSelected` and `--_notSelected` variables, which are useful for styling.
(They can be used in the same way as `--_isHov` and `--_notHov` from [`set--hov`](/en/docs/property-class/hov#set--hov).)

### variant="emboss"

<Preview>
  <PreviewTitle>`variant='emboss'`</PreviewTitle>
  <PreviewArea p="30">
    <Tabs.Root variant="emboss">
      <Tabs.Item>
        <Tabs.Tab>Tab 1</Tabs.Tab>
        <Tabs.Panel>
          <DummyText pre="Tab 01: " />
        </Tabs.Panel>
      </Tabs.Item>
      <Tabs.Item>
        <Tabs.Tab>Tab 2</Tabs.Tab>
        <Tabs.Panel>
          <DummyText length="l" offset={1} pre="Tab 02: " />
        </Tabs.Panel>
      </Tabs.Item>
      <Tabs.Item>
        <Tabs.Tab>Tab 3</Tabs.Tab>
        <Tabs.Panel>
          <DummyText length="l" offset={2} pre="Tab 03: " />
        </Tabs.Panel>
      </Tabs.Item>
    </Tabs.Root>
  </PreviewArea>
  <PreviewCode slot="tab" label="JSX">
    ```jsx 'variant="emboss"'
    <Tabs.Root variant="emboss">
      <Tabs.Item>
        <Tabs.Tab>Tab 1</Tabs.Tab>
        <Tabs.Panel>
          <DummyText pre="Tab 01: " />
        </Tabs.Panel>
      </Tabs.Item>
      <Tabs.Item>
        <Tabs.Tab>Tab 2</Tabs.Tab>
        <Tabs.Panel>
          <DummyText length="l" offset={1} pre="Tab 02: " />
        </Tabs.Panel>
      </Tabs.Item>
      <Tabs.Item>
        <Tabs.Tab>Tab 3</Tabs.Tab>
        <Tabs.Panel>
          <DummyText length="l" offset={2} pre="Tab 03: " />
        </Tabs.Panel>
      </Tabs.Item>
    </Tabs.Root>
    ```
  </PreviewCode>
  <PreviewCode slot="tab" label="HTML">
```html
<div class="b--tabs b--tabs--emboss l--grid">
  <div class="b--tabs_list" role="tablist">
    <button class="b--tabs_tab set--plain" type="button" id="{id}-1-tab" role="tab" aria-controls="{id}-1" aria-selected="true" tabindex="0">Tab 1</button>
    <button class="b--tabs_tab set--plain" type="button" id="{id}-2-tab" role="tab" aria-controls="{id}-2" aria-selected="false" tabindex="-1">Tab 2</button>
    <button class="b--tabs_tab set--plain" type="button" id="{id}-3-tab" role="tab" aria-controls="{id}-3" aria-selected="false" tabindex="-1">Tab 3</button>
  </div>
  <div class="b--tabs_panel" id="{id}-1" role="tabpanel" aria-labelledby="{id}-1-tab" tabindex="0">...Contents...</div>
  <div class="b--tabs_panel" id="{id}-2" role="tabpanel" aria-labelledby="{id}-2-tab" tabindex="0" hidden>...Contents...</div>
  <div class="b--tabs_panel" id="{id}-3" role="tabpanel" aria-labelledby="{id}-3-tab" tabindex="0" hidden>...Contents...</div>
</div>
```
  </PreviewCode>
</Preview>

<PreviewTitle>Required additional CSS</PreviewTitle>
<SrcCode dir="docs" path="src/content/ja/ui/_opt-in/_tabs-emboss.css" />


### variant="folder"

<Preview>
  <PreviewTitle>`variant='folder'`</PreviewTitle>
  <PreviewArea p="30">
    <Tabs.Root variant="folder">
      <Tabs.Item>
        <Tabs.Tab>Tab 1</Tabs.Tab>
        <Tabs.Panel>
          <DummyText pre="Tab 01: " />
        </Tabs.Panel>
      </Tabs.Item>
      <Tabs.Item>
        <Tabs.Tab>Tab 2</Tabs.Tab>
        <Tabs.Panel>
          <DummyText length="l" offset={1} pre="Tab 02: " />
        </Tabs.Panel>
      </Tabs.Item>
      <Tabs.Item>
        <Tabs.Tab>Tab 3</Tabs.Tab>
        <Tabs.Panel>
          <DummyText length="l" offset={2} pre="Tab 03: " />
        </Tabs.Panel>
      </Tabs.Item>
    </Tabs.Root>
  </PreviewArea>
  <PreviewCode slot="tab" label="JSX">
    ```jsx 'variant="folder"'
    <Tabs.Root variant="folder">
      <Tabs.Item>
        <Tabs.Tab>Tab 1</Tabs.Tab>
        <Tabs.Panel>
          <DummyText pre="Tab 01: " />
        </Tabs.Panel>
      </Tabs.Item>
      <Tabs.Item>
        <Tabs.Tab>Tab 2</Tabs.Tab>
        <Tabs.Panel>
          <DummyText length="l" offset={1} pre="Tab 02: " />
        </Tabs.Panel>
      </Tabs.Item>
      <Tabs.Item>
        <Tabs.Tab>Tab 3</Tabs.Tab>
        <Tabs.Panel>
          <DummyText length="l" offset={2} pre="Tab 03: " />
        </Tabs.Panel>
      </Tabs.Item>
    </Tabs.Root>
    ```
  </PreviewCode>
  <PreviewCode slot="tab" label="HTML">
```html
<div class="b--tabs b--tabs--folder l--grid">
  <div class="b--tabs_list" role="tablist">
    <button class="b--tabs_tab set--plain" type="button" id="{id}-1-tab" role="tab" aria-controls="{id}-1" aria-selected="true" tabindex="0">Tab 1</button>
    <button class="b--tabs_tab set--plain" type="button" id="{id}-2-tab" role="tab" aria-controls="{id}-2" aria-selected="false" tabindex="-1">Tab 2</button>
    <button class="b--tabs_tab set--plain" type="button" id="{id}-3-tab" role="tab" aria-controls="{id}-3" aria-selected="false" tabindex="-1">Tab 3</button>
  </div>
  <div class="b--tabs_panel" id="{id}-1" role="tabpanel" aria-labelledby="{id}-1-tab" tabindex="0">...Contents...</div>
  <div class="b--tabs_panel" id="{id}-2" role="tabpanel" aria-labelledby="{id}-2-tab" tabindex="0" hidden>...Contents...</div>
  <div class="b--tabs_panel" id="{id}-3" role="tabpanel" aria-labelledby="{id}-3-tab" tabindex="0" hidden>...Contents...</div>
</div>
```
  </PreviewCode>
</Preview>

This creates a folder-style variation where the tab buttons appear connected to the panel.

<PreviewTitle>Required additional CSS</PreviewTitle>
<SrcCode dir="docs" path="src/content/ja/ui/_opt-in/_tabs-folder.css" />


### Fixed height

An example of how to fix the overall tabs height to match the tallest content panel.

<Preview>
  <PreviewTitle>Example (height is fixed to Tab 2)</PreviewTitle>
  <PreviewArea p="30">
    <Tabs.Root data-tabs-keep-height g="15">
      <Tabs.Item>
        <Tabs.Tab px="10">Tab 1</Tabs.Tab>
        <Tabs.Panel>
          <DummyText pre="Tab 01: " />
        </Tabs.Panel>
      </Tabs.Item>
      <Tabs.Item>
        <Tabs.Tab px="10">Tab 2</Tabs.Tab>
        <Tabs.Panel>
          <DummyText length="xl" offset={1} pre="Tab 02: " />
        </Tabs.Panel>
      </Tabs.Item>
      <Tabs.Item>
        <Tabs.Tab px="10">Tab 3</Tabs.Tab>
        <Tabs.Panel>
          <DummyText length="l" offset={2} pre="Tab 03: " />
        </Tabs.Panel>
      </Tabs.Item>
    </Tabs.Root>
  </PreviewArea>
  <PreviewCode slot="tab" label="JSX">
    ```jsx "data-tabs-keep-height"
    <Tabs.Root data-tabs-keep-height g="15">
      <Tabs.Item>
        <Tabs.Tab px="10">Tab 1</Tabs.Tab>
        <Tabs.Panel>
          <DummyText pre="Tab 01: " />
        </Tabs.Panel>
      </Tabs.Item>
      <Tabs.Item>
        <Tabs.Tab px="10">Tab 2</Tabs.Tab>
        <Tabs.Panel>
          <DummyText length="xl" offset={1} pre="Tab 02: " />
        </Tabs.Panel>
      </Tabs.Item>
      <Tabs.Item>
        <Tabs.Tab px="10">Tab 3</Tabs.Tab>
        <Tabs.Panel>
          <DummyText length="l" offset={2} pre="Tab 03: " />
        </Tabs.Panel>
      </Tabs.Item>
    </Tabs.Root>
    ```
  </PreviewCode>
  <PreviewCode slot="tab" label="HTML">
```html
<div class="b--tabs b--tabs--default l--grid -g:15" data-tabs-keep-height="true">
  <div class="b--tabs_list" role="tablist">
    <button class="b--tabs_tab set--plain -px:10" type="button" id="{id}-1-tab" role="tab" aria-controls="{id}-1" aria-selected="true" tabindex="0">Tab 1</button>
    <button class="b--tabs_tab set--plain -px:10" type="button" id="{id}-2-tab" role="tab" aria-controls="{id}-2" aria-selected="false" tabindex="-1">Tab 2</button>
    <button class="b--tabs_tab set--plain -px:10" type="button" id="{id}-3-tab" role="tab" aria-controls="{id}-3" aria-selected="false" tabindex="-1">Tab 3</button>
  </div>
  <div class="b--tabs_panel" id="{id}-1" role="tabpanel" aria-labelledby="{id}-1-tab" tabindex="0">...Contents...</div>
  <div class="b--tabs_panel" id="{id}-2" role="tabpanel" aria-labelledby="{id}-2-tab" tabindex="0" hidden>...Contents...</div>
  <div class="b--tabs_panel" id="{id}-3" role="tabpanel" aria-labelledby="{id}-3-tab" tabindex="0" hidden>...Contents...</div>
</div>
```
  </PreviewCode>
</Preview>

<PreviewTitle>Required additional CSS</PreviewTitle>
<SrcCode dir="docs" path="src/content/ja/ui/_opt-in/_tabs-keep-height.css" />
