# Authoring Plan出力フォーマット

実装前preflightの成果物をAuthoring Planと呼びます。各行に必ずforward系verdict（✅/⏸/🔁/🆕/⬜）を付け、⏸が残る項目はその部分を実装しないで先にユーザー確認します。

## 軽量レベル

小変更では3〜5行で十分です。

```md
- ✅ 構造: 既存`Stack`内へ1項目追加。Primitive変更なし。
- ✅ token: 既存パターンに合わせ`p="20"`/`fz="s"`を使用。
- 🔁 hover: `property-class/hov.md`を参照し`hov={{ bgc: 'base-2' }}`で実装。
```

## 通常/値照合付きレベル

### 構造表

| 対象領域 | 採用Primitive・Component | 理由 | route先 | 判定 |
|---|---|---|---|---|
| 例: カード一覧 | `Columns` | 等幅3列で、BP切替に向く | `primitive-class.md` | ✅ |

### token表

値照合付きでは`デザイン値`と`差分`列を必須にします。

| 用途 | デザイン値 | 採用token | 差分 | 判定 |
|---|---|---|---|---|
| 余白 | `24px` | `p="30"`候補 | +6px | ⏸ |
| 背景 | — | `bgc="base-2"` | — | ✅ |

### responsive表

| 対象 | base | sm | md | lg | container祖先 | 注意 |
|---|---|---|---|---|---|---|
| カード列数 | 1 | 2 | 3 | — | あり | `cols={[1,2,3]}` |

### 構成表（命名・CSS境界・状態・既定値）

| c--*名 | Props・Property Classで書く | CSSに残す | 状態・variant | 既定で足りる（指定しない） |
|---|---|---|---|---|
| `c--featureCard` | `p="20" bgc="base" bdrs="20"` | `::before`装飾 | `c--featureCard--featured` | `Frame ov`、直下img fit |

### 確認事項表（⏸）

| 項目 | 要確認理由 | 候補 |
|---|---|---|
| `padding:24px` | token近似丸めで見た目差が出る | A=`p="30"`/B=custom token/C=直書き例外 |

## ⏸でユーザー確認する条件

- px/rem/emをtokenへ丸める、tokenを新規追加する、`--s-unit`など基準値を上書きする。
- 任意色・ブランド色・密度・忠実度など、仕様から確定できないデザイン判断をする。
- responsive挙動が見た目・UX・DOM順を変える（固定Grid→可変化、`isContainer`位置変更など）。
- 公開class名・CMS・外部JS・E2Eセレクタに依存する命名/構造変更をする。

## デザインデータ取り込みフロー

1. **候補抽出**: デザイン値（px/色/比率など）ごとに最寄りtokenを1〜2個挙げる。
2. **差分表**: `用途/デザイン値/候補token/差分/判定`で表にする。
3. **判定**:
   - 完全一致→✅そのまま採用。
   - typo・既存規約上明確な置換（`secondary`→`base-2`など）→🔁自動置換。
   - 近似丸め・任意色・ブランド色・密度/忠実度判断→⏸。A=最寄りtokenに丸め/B=custom token化/C=直書き例外を提示する。
4. **custom token化条件**: 同値が複数箇所で再利用される、またはデザインシステム上の意味を持つ場合は候補にする。1回限りの局所値は丸めか例外で済ませる。
5. **デザインデータが無い場合**: 差分表は省略し、既存tokenから意味が近い値を選ぶ。新規数値が必要な時だけ⏸にする。

`--s-unit`などの基準値をプロジェクトが上書きしているとpxの完全一致判定が変わります。値照合付きでは上書きの有無を先に確認してください。

## 提出前セルフチェックへの接続

実装後、Authoring Planと実装を1行ずつ照合します。

| Plan項目 | 実装結果 | 分類 | 対応 |
|---|---|---|---|
| ✅ `Frame ar="16/9"` | 実装済み | OK | なし |
| ✅ `p="20"` | `p="30"`に変更 | 計画変更 | 理由を記録 |
| ⏸ `24px`丸め | 未確認のまま実装 | 要確認 | 実装を止め確認 |
| ✅ `data-is-active` | CSSだけ`is--active`のまま | 実装漏れ | CSS/JS/testを同時修正 |
