---
import type { HTMLTag, Polymorphic } from 'astro/types';
import type { AstroLismBaseProps } from 'lism-css/astro';
import { Lism } from 'lism-css/astro';
import { defaultProps } from '../getProps';

type Props<Tag extends HTMLTag = 'div'> = Polymorphic<{ as: Tag }> & AstroLismBaseProps;

const { as: asTag = 'div' as HTMLTag, ...restProps } = Astro.props;
---

<Lism as={asTag} {...defaultProps.body as Record<string, unknown>} {...restProps}>
  <slot />
</Lism>
