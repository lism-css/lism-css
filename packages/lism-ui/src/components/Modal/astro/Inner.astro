---
// import type { LismProps } from 'lism-css/types';
import { Lism } from 'lism-css/astro';
import { getInnerProps } from '../getProps';

const { class: className, ...props } = Astro.props || {};
---

<Lism {...getInnerProps({ className, ...props })}><slot /></Lism>
