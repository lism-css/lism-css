---
import type { HTMLTag, Polymorphic } from 'astro/types';
import type { AstroLismBaseProps } from 'lism-css/astro';
import { Flex } from 'lism-css/astro';
import atts from 'lism-css/lib/helper/atts';

type Props<Tag extends HTMLTag = 'a'> = Polymorphic<{ as: Tag }> &
  AstroLismBaseProps & {
    href?: string;
  };

const { class: className, as = 'a', ...props } = Astro.props;
---

<Flex as={as} class={atts(className, 'c--navMenu_link')} c="inherit" {...props}><slot /></Flex>
