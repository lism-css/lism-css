---
import { Lism } from 'lism-css/astro';
import { getItemProps } from '../getProps';

const { class: className, ...props } = Astro.props;
---

<Lism {...getItemProps({ className, ...props })}><slot /></Lism>
