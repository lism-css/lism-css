---
import { Lism } from 'lism-css/astro';
import { getTitleProps } from '../getProps';

const { class: className, ...props } = Astro.props || {};
---

<Lism {...getTitleProps({ className, ...props })}><slot /></Lism>
