---
// scriptを使うので ref の関係で <Lism> ではなく getLismProps() を使う
import getLismProps from 'lism-css/lib/getLismProps';
import { getAccProps } from '../getProps';
import uuid from '../../../helper/uuid.js';

import '../_style.css';

// const accID = tabId ? tabId : uuid();

// Propsの定義
// interface Props extends LismProps {
// 	duration?: string | number;
// }
const props = Astro.props || {};
---

<div {...getLismProps(getAccProps(props))}>
	<slot />
</div>

<script>
	import setAccordion from '../setAccordion';
	setAccordion();
</script>
