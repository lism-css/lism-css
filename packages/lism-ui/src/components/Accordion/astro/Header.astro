---
// import type { LismProps } from 'lism-css/types';
import { Lism } from 'lism-css/astro';
import { getHeadingProps } from '../getProps';

// interface Props extends LismProps {}

const props = Astro.props || {};
---

<Lism {...getHeadingProps(props)}>
	<slot />
</Lism>
