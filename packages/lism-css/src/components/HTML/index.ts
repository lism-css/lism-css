/**
 * @deprecated Text, Inline, Group, Heading, Link, List, ListItem, Media を使用してください。
 * このコンポーネントは将来のバージョンで削除されます。
 */
import { div, p, span, img, a, h, ul, ol, li, button } from './HTML';

const HTML = { div, p, span, img, a, h, ul, ol, li, button };

export default HTML;
