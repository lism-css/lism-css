---
import type { LismComponentProps } from '../../../src/components/Lism/Lism';
import { Lism } from '../Lism';
import getFilterProps, { type FilterProps } from '../../../src/components/getFilterProps';

type Props = LismComponentProps & FilterProps;
---

<Lism isLayer {...getFilterProps(Astro.props, 'backdropFilter')}>
	<slot />
</Lism>
