---
import type { AstroLismBaseProps } from '../types';
import Lism from '../Lism/Lism.astro';

type PAttrs = Omit<astroHTML.JSX.HTMLAttributes, keyof AstroLismBaseProps>;
interface Props extends AstroLismBaseProps, PAttrs {}

const props = Astro.props;
---

<Lism as='p' {...props}><slot /></Lism>
